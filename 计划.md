彬，我看了你现在这个项目的结构、附件 md、还有这张异常图。先给结论：**你这个项目可以借鉴那篇 PNAS 论文的“沉淀模式判别思路”，但不能直接复现它的高 Re 惯性机制；如果你想走 PHREEQC 路线，建议先做“输运代码 + PHREEQC 反应模块”的 operator splitting，而不是一上来重写整个流体求解器。**

---

# 1. 你的项目怎么借鉴这篇论文做“沉淀模拟”？

你现在的项目本质上是：**孔隙尺度反应输运 + level set 界面演化 + 溶解导致孔隙率/渗透率变化 + 后续 COMSOL/NMR 数据生成**。项目文档里也写得很清楚，当前主目标是生成 RTM pore-scale dissolution samples，再接 COMSOL/NMR 和 T2 inversion 数据流。

你现在代码里核心反应还是偏 **H⁺ 驱动矿物溶解**：

[
r_{\mathrm{diss}} = c_H \cdot k
]

对应代码中：

```matlab
dissolutionReactionRate = @(cHydrogen) ...
    (cHydrogen * 1000 * rateCoefficientTST);
```



所以你要从“溶解”扩展到“沉淀”，有两条路线。

---

## 路线 A：借鉴 PNAS 论文，先做“表面沉淀倾向图”

这条路线最稳，最适合你现在快速做出论文里能讲清楚的结果。

PNAS 那篇文章真正做的不是完整的“沉淀物不断长大并实时堵塞通道”的全耦合模型，而是：**求稳态流场和稳态溶质输运，然后用壁面饱和比 (\Omega_{\mathrm{Surf}}) 判断哪里更容易发生表面沉淀**。它明确说，模拟中没有考虑沉淀导致的通道几何变化。

你可以把这套思想迁移成：

[
\text{流场}
\rightarrow
\text{Ba}^{2+},\ \text{SO}*4^{2-}\text{输运}
\rightarrow
\Omega=\frac{c*{\mathrm{Ba}}c_{\mathrm{SO_4}}}{K_{sp}}
\rightarrow
R_s=k(\Omega-1)
\rightarrow
\text{表面沉淀强度图}
]

其中：

[
R_s =
\begin{cases}
k(\Omega-1), & \Omega>1 \
0, & \Omega\leq 1
\end{cases}
]

你现在图里已经有三个非常适合扩展的模块：上面是浓度场，中间是矿物界面，下面是流速场。你只需要再增加一个后处理量：

[
\Omega_{\mathrm{surface}}
]

或者：

[
R_{\mathrm{precip,surface}}
]

然后输出类似 PNAS 的：

| 输出图                                      | 作用             |
| ---------------------------------------- | -------------- |
| 浓度场 (c_{\mathrm{Ba}}, c_{\mathrm{SO_4}}) | 看反应物是否混合       |
| 表面饱和比 (\Omega_{\mathrm{Surf}})           | 看哪里有沉淀潜力       |
| 表面反应速率 (R_s)                             | 看哪里沉淀最快        |
| 流线/速度场                                   | 解释为什么沉淀区域出现在这里 |
| 孔隙率/渗透率随时间变化                             | 说明是否发生堵塞趋势     |

这一步甚至可以**先不更新几何**。也就是说，你先做一个“沉淀风险预测模型”，验证不同流速、不同孔隙结构下，表面沉淀区怎么变化。

这个阶段很适合写成论文故事：

> Inertial or advective transport modifies reactant accessibility to mineral surfaces, resulting in spatially heterogeneous precipitation potential.

不过这里要注意：你当前是 **2D Stokes/简化流动**，而 PNAS 的核心机制是 **3D inertial flow / Dean flow / recirculation**。所以你可以借鉴它的“表面可及性 + (\Omega_{\mathrm{Surf}})”框架，但暂时不要声称自己复现了它的三维惯性机制。

---

## 路线 B：在你的 level set 里真正加入“沉淀导致孔隙缩小”

这条路线更接近你自己的项目特色。

你现在已有 level set 描述固液界面；附件 md 里也总结了 RTSPHEM-P 的逻辑：宏观流动和运移，微观 REV 中追踪矿物溶解/沉淀导致的孔隙结构演化，并把孔隙率、渗透率、有效扩散张量、反应比表面积反馈给宏观方程。

对于沉淀，你只需要把界面速度从“固体溶解、孔隙扩大”改成“固体增长、孔隙缩小”。

可以写成：

[
v_n = V_m r
]

但是要特别注意符号。你现在 level set 大概率是：

| 区域   | level set 符号 |
| ---- | ------------ |
| 孔隙流体 | (L<0)        |
| 固体矿物 | (L>0)        |
| 固液界面 | (L=0)        |

那么溶解和沉淀的界面推进方向刚好相反：

| 过程 | 物理效果      | level set 变化 |
| -- | --------- | ------------ |
| 溶解 | 固体变小，孔隙扩大 | 界面向固体内部退缩    |
| 沉淀 | 固体变大，孔隙缩小 | 界面向孔隙内部推进    |

所以你后续写代码时，最容易出 bug 的地方就是 **(v_n) 的符号**。建议你先做一个最简单测试：单个圆形矿物颗粒，在均匀过饱和场里沉淀，观察圆是否变大。如果圆变小，说明符号反了。

---

## 路线 C：保留当前流动和输运，用 PHREEQC 做反应模块

这条路线我觉得是你后面最有潜力的，因为它能把项目从“自己写几个简单动力学公式”升级成“可处理多组分地球化学反应”。

逻辑是：

[
\text{HyPHM/当前代码求流场}
\rightarrow
\text{当前代码求总组分输运}
\rightarrow
\text{每个网格/界面点调用 PHREEQC}
\rightarrow
\text{得到 SI、矿物沉淀/溶解量、组分更新}
\rightarrow
\text{更新 level set 与孔隙结构}
]

这其实就是你附件 md 里 RTSPHEM-P 的思想：**输运模块和化学反应模块分开，用 operator splitting / SNIA 耦合**。GeoChemFoam 也采用类似路线：输运步骤用 OpenFOAM 求解，反应步骤用 Phreeqc 求解，并通过 sequential operator splitting 耦合；它面向复杂孔隙几何、微流控和 micro-CT 图像。([arXiv][1])

你可以把 PHREEQC 作为“反应黑箱”：

输入给 PHREEQC：

| 输入                         | 说明                        |
| -------------------------- | ------------------------- |
| 总 Ca、C、Ba、S、H、O 等组分        | 由输运方程更新                   |
| pH / H⁺                    | 由输运或 PHREEQC 平衡计算         |
| 温度                         | 可先固定 25 °C                |
| 矿物相                        | Calcite、Barite、Dolomite 等 |
| kinetic/equilibrium phases | 控制溶解/沉淀                   |
| 当前矿物量                      | 与 level set / 固相体积分数对应    |

PHREEQC 输出：

| 输出                           | 用途       |
| ---------------------------- | -------- |
| saturation index, SI         | 判断是否过饱和  |
| amount of mineral reacted    | 换算成界面移动量 |
| updated total concentrations | 回写输运模块   |
| pH / species activities      | 用于下一步反应  |

我建议你的实现顺序是：

**第一阶段：只用 PHREEQC 计算每个点的 SI，不更新浓度。**
比如每个时间步后，把 (c_{\mathrm{Ba}})、(c_{\mathrm{SO_4}})、pH 丢给 PHREEQC，输出 Barite 的 SI。先画出 SI 分布。

**第二阶段：用 PHREEQC 计算沉淀量，但不更新几何。**
验证质量守恒，避免一上来 level set 和化学都耦合导致无法排错。

**第三阶段：把沉淀量转成 level set 界面移动。**
例如：

[
\Delta V_{\mathrm{solid}}
=========================

n_{\mathrm{precip}} V_m
]

[
\Delta h
========

\frac{\Delta V_{\mathrm{solid}}}{A_{\mathrm{interface}}}
]

再把 (\Delta h) 转成 level set 位移。

---

# 2. 你现在高流速崩坏的核心原因是什么？

你图里上面的浓度场出现了明显异常：下游有红蓝相间的条纹，局部像是被数值震荡“撕裂”了一样。这个现象大概率不是化学反应本身，而是 **高 Pe / 高 Re 下输运格式和流场模型都不再适配**。

你的代码自己其实已经在提醒这个问题。`PNM_beauty3.m` 中计算了：

[
Re=\frac{uL}{\nu}
]

并且代码写了：如果 (Re<1)，输出 “Stokes flow valid”；否则输出 “Inertial effects may appear”。

这说明你当前底层逻辑默认仍然是：

> 低 Re，Stokes / creeping flow 合理。

但你现在想把流速调高，这时会同时触发两个问题。

---

## 问题 1：Stokes 方程不包含惯性项

Stokes 本质上忽略了：

[
\mathbf{u}\cdot\nabla \mathbf{u}
]

所以它适用于：

[
Re \ll 1
]

而 PNAS 那篇论文研究的恰恰是：

[
Re=10\sim150
]

也就是惯性层流。这个区间里，二维 Stokes 解不能给出 Dean flow、recirculation、z 向流动，也就不能解释那篇文章里“高 Re 反而更容易堵塞”的核心机制。

你仓库里其实有 HyPHM 的 `NavierStokes` 类，不是完全没有。它的求解器里有 convective term 的装配逻辑，`d.isConvection` 开启时会装配额外项，并用迭代更新速度场。 但这不是一个即插即用的稳定方案，因为代码里也明确写了：如果 Newton 对流项不收敛，会直接报错，并建议 refine spatial and/or temporal decomposition。

所以我的判断是：

> 你当前 repo 里有 Navier–Stokes 的雏形，但你的 RTM 主流程还不是围绕稳健 Navier–Stokes 高 Re 模拟设计的。短期内不要指望改一个开关就能稳定跑高流速。

---

## 问题 2：浓度输运在高 Pe 下会数值震荡

你现在代码会计算 Péclet number：

[
Pe=\frac{uL}{D}
]

并根据 Pe 和 Da 自动设置时间步。 但这还不够，因为高 Pe 时，对流占主导，中心格式很容易出现非物理震荡。

HyPHM 的 `Transport` 类其实已经提供了上风稳定选项。文档里写了，当局部 Pe 较大时，需要 stabilization 来抑制 spurious oscillations；并提供了 `'full'`、`'exp'`、`'alt'` 等 upwinding 策略，而默认是 `'none'`。 代码中也能看到 `isUpwind` 默认值就是 `'none'`。

所以你图里的浓度异常，我优先怀疑：

1. **Transport 没有启用 upwind / flux limiter；**
2. **时间步虽然根据 Pe 调整了，但没有严格满足局部 CFL；**
3. **几何喉道太窄，局部速度远高于入口平均速度；**
4. **level set 更新造成细小孤立孔隙或尖角，导致矩阵病态；**
5. **浓度没有做非负性保护，出现 overshoot / undershoot 后被色图放大。**

---

# 3. “奇异矩阵”一般从哪里来？

你现在遇到的 singular matrix，大概率不是单一原因，而是下面几类叠加。

## 3.1 压力约束不唯一或过约束

Stokes 求解器中，压力需要唯一性约束。代码里如果没有 Neumann boundary 且没有 `balanceP`，会提醒需要 pressure balance；如果同时有 Neumann 边界又设置了 balanceP，也可能过约束。

对应到你的项目里，要检查：

| 检查项                | 风险              |
| ------------------ | --------------- |
| 入口/出口/壁面边界是否都分配了类型 | 边界缺失会直接出问题      |
| 压力参考点或平均压力约束是否设置   | 没有会导致压力不唯一      |
| 是否同时设置了互相冲突的压力约束   | 可能导致过约束         |
| 孔隙是否断开成多个孤立区域      | 每个连通域都需要约束，否则奇异 |

Stokes solver 也要求所有边界 edge 都必须被分配为 Dirichlet 或 Neumann，否则会 assert。

---

## 3.2 几何演化导致“断喉”或孤立孔隙

你图里矿物颗粒周围已经形成很窄的流道。高流速下局部速度会在窄喉处被放大，反应/沉淀/溶解又会改变界面，一旦出现：

| 几何问题             | 后果         |
| ---------------- | ---------- |
| 孔喉宽度接近网格尺度       | 局部矩阵病态     |
| 孔隙被断开            | 压力/流速解不唯一  |
| 出现孤立孔洞           | 输运和流动矩阵奇异  |
| level set 没有重初始化 | 法向量、界面速度异常 |
| 单个时间步界面移动超过一个网格  | 几何翻转、负孔隙率  |

所以必须加几个保护：

[
\Delta t_{\mathrm{interface}}
<
C
\frac{h}{|v_n|}
]

建议 (C=0.1\sim0.3)。

也就是说，时间步不仅要满足输运 CFL，还要满足 **界面移动 CFL**。

---

# 4. 有没有人已经解决了？开源里有，但不是你当前代码的小修小补

我的判断分三层。

## 层 1：HyPHM/你当前 RTSPHEM-main 里，有部分工具，但不完整

你仓库里确实有：

| 模块             | 状态                                      |
| -------------- | --------------------------------------- |
| `Stokes`       | 成熟一些，当前更像主力                             |
| `Transport`    | 有 upwind 选项，但默认未启用                      |
| `NavierStokes` | 有对流项和 Newton 迭代，但稳定性需要自己调               |
| `PNM_beauty3`  | 当前主线是 pore-scale dissolution + NMR 数据生成 |
| PHREEQC 耦合     | 当前 repo 里我没有看到已经完整接入主流程                 |

所以，**在你当前项目里，还没有一个已经稳定整合好的“高 Re Navier–Stokes + 多组分 PHREEQC + level set 沉淀堵塞”的现成模块**。

## 层 2：开源领域里，有成熟方向

如果你愿意迁移或借鉴外部框架，最接近的是 **GeoChemFoam / OpenFOAM + PHREEQC** 这条路线。GeoChemFoam 面向真实孔隙几何、microfluidic devices 和 micro-CT 图像，输运用 OpenFOAM，反应用 Phreeqc，并采用 sequential operator splitting。([arXiv][1])

OpenFOAM 本身是开源 CFD 工具箱，主要用于连续介质问题和 CFD 求解。([维基百科][2])

所以如果你的目标是严格复现 PNAS 的高 Re 惯性流：

> 更现实的路线是：OpenFOAM / GeoChemFoam / COMSOL 做流体与输运，你自己的 MATLAB 代码做几何生成、后处理、NMR 数据链。

## 层 3：对你当前论文/项目最现实的选择

我不建议你现在立刻重构成 OpenFOAM。成本太高，而且会把你的论文推进拖住。

更稳的路线是：

> 低到中等 Re：继续用当前代码，重点做溶解/沉淀、孔隙率、渗透率、NMR 数据生成。
> 高 Re 惯性机制：先作为对照讨论，不强行用当前 Stokes 代码模拟。
> PHREEQC：作为第二阶段增强模块接入。

---

# 5. 我建议你接下来这样改

## 第一优先级：先把当前代码稳定住

你先不要急着加 PHREEQC，也不要急着高 Re。

先加 6 个诊断量：

| 诊断量                        | 目的               |
| -------------------------- | ---------------- |
| `min(c)` / `max(c)`        | 检查浓度是否负值或爆炸      |
| global mass balance        | 检查质量守恒           |
| local max velocity         | 不只看入口速度，要看孔喉最大速度 |
| local Pe                   | 判断哪里会震荡          |
| CFL = (u_{\max}\Delta t/h) | 控制输运稳定性          |
| connected pore components  | 检查是否出现孤立孔隙       |

尤其是每一步都打印：

[
c_{\min},\ c_{\max},\ Pe_{\max},\ CFL_{\max},\ \phi,\ K
]

只要 (c_{\min}<0) 或 (CFL_{\max}>1)，这一组结果就不能相信。

---

## 第二优先级：启用 transport upwinding

你可以先尝试把 HyPHM Transport 的：

```matlab
isUpwind = 'none'
```

改为：

```matlab
isUpwind = 'alt'
```

或者在高 Pe 情况下直接用：

```matlab
isUpwind = 'full'
```

因为 Transport 文档明确说，高局部 Pe 下需要上风稳定来抑制数值震荡，默认 `'none'` 是最容易出问题的。

我的建议是：

| 模式       | 使用场景              |
| -------- | ----------------- |
| `'none'` | 低 Pe 验证、扩散主导      |
| `'alt'`  | 推荐默认，用于 Pe 有局部升高  |
| `'full'` | 高 Pe，先求稳定，不追求最高精度 |
| `'exp'`  | 后面做对比用            |

---

## 第三优先级：把时间步从“经验 Pe-Da”改成“硬 CFL 限制”

你当前 `PNM_beauty3` 里已经根据 Pe 和 Da 自动设 `maximalStep`。 但我建议再加一个硬约束：

[
\Delta t
\leq
C_{\mathrm{adv}}
\frac{h_{\min}}{u_{\max}}
]

[
\Delta t
\leq
C_{\mathrm{react}}
\frac{h_{\min}}{|v_n|_{\max}}
]

取：

[
C_{\mathrm{adv}}=0.2\sim0.5
]

[
C_{\mathrm{react}}=0.1\sim0.3
]

这样可以直接避免你图里这种浓度条纹和界面跳变。

---

## 第四优先级：先做 PNAS-style 沉淀后处理，不急着几何更新

你可以先加一个函数：

```matlab
Omega = cBa .* cSO4 ./ Ksp;
Rprec = kprec .* max(Omega - 1, 0);
```

然后只在固液界面附近采样：

```matlab
interfaceMask = abs(levelSet) < eps_interface;
Rsurf = Rprec .* interfaceMask;
```

输出：

1. `Omega_surface.png`
2. `Rprec_surface.png`
3. `precipitation_risk_area.csv`
4. `porosity/permeability` 暂时不变

这样你会很快得到一版能讲故事的结果：

> 不同流速、不同孔隙结构下，沉淀倾向区从入口/界面附近向孔喉或下游扩展。

---

## 第五优先级：再做 PHREEQC 耦合

PHREEQC 模块建议做成独立接口，不要直接写死在主循环里。

建议文件结构：

```text
ReactiveTransport/
  chemistry/
    phreeqc_interface.m
    build_phreeqc_input.m
    parse_phreeqc_output.m
    run_cell_chemistry.m
```

主循环只需要调用：

```matlab
chem = run_cell_chemistry(totalComponents, minerals, temperature, dt);
```

返回：

```matlab
chem.SI
chem.deltaMineralMoles
chem.updatedComponents
chem.pH
```

这样以后你可以替换 PHREEQC、Reaktoro 或手写 TST 公式，不会动主程序。

---

# 6. 你现在最适合的技术路线

我给你一个比较现实的版本规划。

## Version 1：稳定版溶解模型

目标：把当前 H⁺ 溶解模型稳定跑通。

必须完成：

1. 启用 upwind；
2. 加 CFL 限制；
3. 加非负浓度保护；
4. 加连通域检查；
5. 加 mass balance 输出；
6. 避免高 Re，控制 (Re<1)。

这一版可以作为你后面所有扩展的基线。

---

## Version 2：PNAS-style 表面沉淀风险模型

目标：不更新几何，只预测沉淀区。

新增：

[
\Omega_{\mathrm{Surf}}
]

[
R_{\mathrm{precip}}
]

输出不同流速、不同孔隙结构下的沉淀热点图。

这一版最容易做成论文图。

---

## Version 3：level set 沉淀堵塞模型

目标：沉淀导致孔隙缩小，出现堵塞。

新增：

[
v_n = - V_m R_{\mathrm{precip}}
]

然后更新 level set。

重点是控制：

[
\Delta t < C h / |v_n|
]

否则很容易几何翻转或矩阵奇异。

---

## Version 4：PHREEQC 多组分反应模型

目标：从简单公式扩展到真实地球化学。

流程：

[
\text{Transport}
\rightarrow
\text{PHREEQC}
\rightarrow
\text{SI / mineral amount}
\rightarrow
\text{level set}
\rightarrow
\text{flow update}
]

这一版更适合后续高水平论文，但实现成本也最高。

---

# 7. 最关键的判断

你现在不要把问题理解成“代码崩了，所以要找一个现成项目替换”。更准确地说：

**你的代码当前适合做低 Re、孔隙尺度、界面演化、溶解/沉淀导致孔隙结构变化的研究；但不适合直接做 PNAS 那种 Re = 10–150 的三维惯性沉淀机制。**

所以你的最佳策略是：

> 当前 MATLAB/HyPHM 代码负责：孔隙结构演化 + 反应输运 + NMR 数据生成。
> PNAS 论文负责：提供“表面可及性、(\Omega_{\mathrm{Surf}})、沉淀模式判别”的理论框架。
> PHREEQC 负责：未来增强真实多组分化学。
> 高 Re / 3D 惯性流如果真的要做，另开 OpenFOAM/COMSOL 分支，不要硬塞进当前 Stokes 主线。

我觉得你下一步最该做的是：**把当前代码的 transport 稳定性修好，然后先实现 (\Omega_{\mathrm{Surf}}) 和 (R_{\mathrm{precip}}) 后处理图。** 这一步最短，也最容易产生能放进论文里的结果。

[1]: https://arxiv.org/abs/2103.03579?utm_source=chatgpt.com "GeoChemFoam: Direct modelling of multiphase reactive transport in real pore geometries with equilibrium reactions"
[2]: https://en.wikipedia.org/wiki/OpenFOAM?utm_source=chatgpt.com "OpenFOAM"
