下面是重写后的计划：**新增 iVoS/DBS + PHREEQC `EQUILIBRIUM_PHASES` solver**，保留原始 GeoChemFoam 功能不变。

**目标**
新增一套独立功能：

`reactiveTransportDBSPhreeqcEqFoam`

用于计算：

`Ca+2, HCO3-, CO3-2, H+, OH-` 多物种输运  
`+ PHREEQC EQUILIBRIUM_PHASES Calcite`  
`+ calcite 溶解/沉淀导致 eps 更新`

不修改原来的：
- `reactiveTransportDBSFoam`
- `reactiveTransportALEFoam`
- 原 calcitePostDBS tutorial
- 原 `reactiveMixture`
- 原 `phreeqcMixture`

只新增 solver、mixture、case 和后处理工具。

**总体架构**
新增三层：

1. **Species Transport 层**
   OpenFOAM 负责显式输运：
   - `Ca+2`
   - `HCO3-`
   - `CO3-2`
   - `H+`
   - `OH-`

2. **PHREEQC Equilibrium 层**
   每个 cell 调用 PHREEQC：
   - 读取输运后的物种浓度；
   - 根据 carbonate equilibrium 重新配平；
   - 用 `EQUILIBRIUM_PHASES Calcite` 判断溶解或沉淀；
   - 返回更新后的水相物种浓度、pH、SI、calcite 剩余量。

3. **iVoS/DBS Geometry 层**
   用 PHREEQC 返回的 calcite 增减量更新：
   - `calciteMoles`
   - `eps`
   - permeability / drag term

**新增文件建议**
新增 solver：

```text
GeoChemFoam-5.2/applications/solvers/reactiveTransport/reactiveTransportDBSPhreeqcEqFoam/
  reactiveTransportDBSPhreeqcEqFoam.C
  createFields.H
  UEqn.H
  pEqn.H
  speciesEqn.H
  phreeqcEqStep.H
  epsFromCalcite.H
  Make/files
  Make/options
```

新增 mixture：

```text
GeoChemFoam-5.2/src/thermophysicalModels/reactionThermo/mixtures/phreeqcEquilibriumPhaseMixture/
  phreeqcEquilibriumPhaseMixture.H
  phreeqcEquilibriumPhaseMixture.C
```

新增工具/字段逻辑：

```text
GeoChemFoam-5.2/src/thermophysicalModels/reactionThermo/mixtures/phreeqcEquilibriumPhaseMixture/
  carbonateSpeciesMap.H
  carbonateSpeciesMap.C
  calciteMassMapper.H
  calciteMassMapper.C
```

新增 tutorial：

```text
GeoChemFoam-5.2/tutorials/reactiveTransport/reactiveTransportDBSPhreeqcEqFoam/3DcalcitePostDBSPhreeqcEq/
  0/
    Ca+2
    HCO3-
    CO3-2
    H+
    OH-
    eps
    p
    U
  constant/
    GeoChem.dat
    phreeqcReactions
    transportProperties
    thermoPhysicalProperties
  system/
    controlDict
    fvSchemes
    fvSolution
```

**阶段 1：PHREEQC 0D 基准**
先做纯 PHREEQC 输入，不接 OpenFOAM。

目标：验证 `EQUILIBRIUM_PHASES Calcite` 对欠饱和/过饱和水都能正确处理。

示例输入：

```phreeqc
SOLUTION 0
    temp 25
    units mol/L
    pH 4 charge
    Ca 1e-6
    C(4) 1e-3

EQUILIBRIUM_PHASES 0
    Calcite 0 1.0

SELECTED_OUTPUT
    -pH true
    -si Calcite
    -equilibrium_phases Calcite
    -molalities Ca+2 HCO3- CO3-2 H+ OH-
END
```

验收：
- 欠饱和时 calcite 减少；
- 过饱和时 calcite 增加；
- `SI(Calcite)` 接近 0；
- `Ca+2/HCO3-/CO3-2/H+/OH-` 都能输出。

**阶段 2：定义物种字段和单位**
OpenFOAM 字段名建议不要直接用 `Ca+2` 作为文件名，因为 `+`、`-` 在 OpenFOAM/脚本里容易麻烦。

建议内部字段名：

```text
Ca
HCO3
CO3
H
OH
```

PHREEQC 映射：

```text
Ca   -> Ca+2
HCO3 -> HCO3-
CO3  -> CO3-2
H    -> H+
OH   -> OH-
```

所有字段统一用：

```text
mol/L 或 kmol/m3
```

建议在代码里集中转换：

```text
OpenFOAM kmol/m3 <-> PHREEQC mol/L
```

注意：

```text
1 kmol/m3 = 1 mol/L
```

所以如果 OpenFOAM 字段用 `kmol/m3`，数值上可以和 PHREEQC `mol/L` 对齐，但仍然要在代码里明确写清楚单位，避免以后错。

**阶段 3：新增 `calciteMoles` 字段**
新增一个固相矿物量字段：

```text
calciteMoles
```

每个 cell 的关系：

```text
solidVolume = (1 - eps) * Vcell
calciteMoles = solidVolume * rhoCalcite / MwCalcite
```

推荐单位：

```text
rhoCalcite = 2710 kg/m3
MwCalcite  = 0.1000869 kg/mol
calciteMoles = mol
```

更新回 `eps`：

```text
solidVolume = calciteMoles * MwCalcite / rhoCalcite
eps = 1 - solidVolume / Vcell
eps = clamp(eps, epsMin, 1.0)
```

验收：
`eps -> calciteMoles -> eps` 往返误差小于 `1e-12` 量级。

**阶段 4：新增 PHREEQC equilibrium mixture**
新增 `phreeqcEquilibriumPhaseMixture`，不要改原 `phreeqcMixture`。

它负责：

1. 创建 PhreeqcRM instance。
2. 加载 `constant/GeoChem.dat`。
3. 加载 `constant/phreeqcReactions`。
4. 为每个 cell 初始化 `SOLUTION`。
5. 为每个含固相 cell 初始化：

```phreeqc
EQUILIBRIUM_PHASES cellId
    Calcite 0 calciteMoles
```

6. 每个时间步：
   - 从 OpenFOAM 读取 `Ca/HCO3/CO3/H/OH`；
   - 写入 PHREEQC；
   - 写入当前 `calciteMoles`；
   - 调用 `RM_RunCells`；
   - 取回更新后的 `Ca+2/HCO3-/CO3-2/H+/OH-`；
   - 取回 `SI(Calcite)`；
   - 取回新的 calcite phase amount；
   - 计算 `deltaCalciteMoles`。

输出字段：
```text
Ca
HCO3
CO3
H
OH
pH
SI_Calcite
calciteMoles
deltaCalciteMoles
```

**阶段 5：新增 solver 主循环**
新 solver 主循环：

```text
read fields
while runTime.loop():
    solve flow U/p
    solve transport for Ca, HCO3, CO3, H, OH
    run PHREEQC equilibrium
    update Ca, HCO3, CO3, H, OH
    update calciteMoles
    update eps from calciteMoles
    update Kinv/permeability from eps
    write fields
```

关键点：  
PHREEQC step 必须在 species transport 之后，eps update 之前。

**阶段 6：calcite 活性区域控制**
这是必须做的，否则 `EQUILIBRIUM_PHASES` 可能让 calcite 在整个流体区域沉淀。

新增字段：

```text
calciteActive
```

规则：

```text
calciteActive = 1 if eps < 0.999 or near solid interface
calciteActive = 0 otherwise
```

PHREEQC 只在 `calciteActive=1` 的 cell 里允许：

```phreeqc
EQUILIBRIUM_PHASES Calcite
```

否则 cell 只做水相 speciation，不允许无中生有沉淀。

如果你希望模拟成核沉淀，可以以后单独加 nucleation rule。第一版不要加。

**阶段 7：沉淀处理**
`EQUILIBRIUM_PHASES` 会允许沉淀。沉淀时：

```text
deltaCalciteMoles > 0
solidVolume increases
eps decreases
```

需要限制：

```text
eps >= epsMin
```

如果沉淀量超过孔隙空间：

```text
allowedSolidVolume = (1 - epsMin) * Vcell
excessCalcite = requestedCalcite - allowedCalcite
```

处理方案：
- 第一版：截断沉淀，记录 `excessCalciteMoles`；
- 后续：考虑堵塞、成核、表面沉淀位置。

**阶段 8：时间步和稳定性控制**
`EQUILIBRIUM_PHASES` 是瞬时平衡，不是动力学速率。酸性水遇到 calcite 时，单步可能溶掉大量固体。

必须加限制：

```text
maxDeltaEpsPerStep = 0.005 或 0.01
```

如果 PHREEQC 给出的 `deltaEps` 超过限制：

方案 A：缩小时间步。  
方案 B：reaction subcycling。  
方案 C：限制本步矿物变化，把剩余反应留到下一步。

第一版建议用方案 C，简单稳：

```text
deltaEpsLimited = clamp(deltaEps, -maxDeltaEps, +maxDeltaEps)
```

并输出 `deltaEpsClipped`。

**阶段 9：测试路线**
不要直接上 3D。

测试 1：0D PHREEQC  
验证化学平衡。

测试 2：单 cell OpenFOAM  
无流动，只调用 PhreeqcRM，验证和 0D 一致。

测试 3：1D column  
入口酸性水，calcite 初始存在。看：
- pH 是否上升；
- Ca 是否增加；
- calcite 是否减少；
- SI 是否趋近 0。

测试 4：2D 小圆柱 iVoS  
小网格，检查 `eps` 是否在固体附近演化。

测试 5：3D calcitePost  
最后再跑完整案例。

**阶段 10：质量守恒诊断**
每个时间步输出 CSV：

```text
time
totalCaAqueous
totalCarbonAqueous
totalH
totalOH
totalCalciteMoles
deltaCalciteMolesTotal
epsMin
epsMax
massBalanceCaError
massBalanceCError
```

必须检查：

```text
Ca_aqueous + Ca_calcite = conserved, except inlet/outlet flux
C_aqueous + C_calcite = conserved, except inlet/outlet flux
```

对于有入口/出口流动的案例，要加 flux balance：

```text
accumulation = inletFlux - outletFlux + reaction
```

**最可能出现的问题和解决方案**
1. **PHREEQC species 和 OpenFOAM 字段不一一对应**
   PHREEQC 里 `Ca+2`、`HCO3-` 是 species，不是 primary component。  
   解决：第一版可以使用 PHREEQC species-save 模式；字段名用 `Ca/HCO3/CO3/H/OH`，内部映射到 PHREEQC species 名。

2. **电荷不守恒**
   单独输运 `Ca+2/HCO3-/CO3-2/H+/OH-` 后，数值误差会破坏电中性。  
   解决：PHREEQC step 后重新 speciation，并输出 charge imbalance。必要时用 `pH charge` 或 alkalinity 作为约束。

3. **H+ 和 OH- 数值极小，输运不稳定**
   `H+`、`OH-` 浓度可能是 `1e-7` 或更低，容易出现负值。  
   解决：对所有 species 加非负限制：
   ```text
   Yi = max(Yi, small)
   ```
   并对 H/OH 使用更严格的 bounded scheme。

4. **瞬时平衡导致固体突变**
   解决：`maxDeltaEpsPerStep`、subcycling、clipping 诊断。

5. **沉淀位置不合理**
   如果所有 cell 都允许 calcite phase，会到处沉淀。  
   解决：`calciteActive` mask，只在固体或界面附近允许 calcite phase。

6. **eps 和 calciteMoles 不一致**
   解决：每次更新后都由 `calciteMoles` 重新计算 `eps`，不要两套状态各自演化。

7. **PHREEQC 性能太慢**
   解决：只对 active cells 调 PHREEQC；非 active cells 只做简单 speciation 或跳过 equilibrium phases。

8. **数据库 species 名称不匹配**
   不同数据库中 species 名称可能不同。  
   解决：固定使用一个数据库，比如 `phreeqc.dat` 或当前 `GeoChem.dat`，启动时打印 species list 并检查映射是否存在。

**里程碑**
1. 新 solver 能编译，但不改原 solver。
2. 新 PHREEQC mixture 能初始化并打印 species mapping。
3. 单 cell PHREEQC 结果和独立 PHREEQC 基准一致。
4. `calciteMoles <-> eps` 转换通过单元测试。
5. 1D column 质量守恒通过。
6. 2D iVoS 圆柱出现合理溶解/沉淀。
7. 3D calcitePost 可输出每 1000 秒过程图和质量守恒 CSV。

**最终验收**
这个新功能完成时，应证明：

- 原 `reactiveTransportDBSFoam` 仍可运行；
- 新 `reactiveTransportDBSPhreeqcEqFoam` 可编译运行；
- 显式考虑 `Ca+2/HCO3-/CO3-2/H+/OH-`；
- `EQUILIBRIUM_PHASES Calcite` 能产生溶解和沉淀；
- `calciteMoles` 与 `eps` 一致；
- Ca/C 质量守恒能解释；
- 结果图中固体演化合理。