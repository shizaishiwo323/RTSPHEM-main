# 审核结论

**你的工程方向是对的，但当前科学模型还不能称为 Zhang–Yoon 沉淀复现。**

已经做对的部分包括：

* 将沉淀模块隔离在 `ReactiveTransport/RTM/precipitate`；
* 实现上下分流的 CaCl₂/Na₂CO₃ 入口；
* 保留 PHREEQC `KIN_DELTA` 的正负号；
* 验证沉淀和溶解对应的 level-set 运动方向；
* 建立 13、18、118 min 输出、面积曲线、元数据和测试链路。

但是，当前代码本质上模拟的是：

> **“在已有固体表面发生 Calcite 沉淀或溶解，并用 level-set 移动这个表面”**

而 Zhang–Yoon benchmark 的核心是：

> **两股溶液在孔体中央横向混合，在原本没有 CaCO₃ 固体的孔隙水中产生以 Vaterite 为主的新沉淀，并以局部沉淀体积分数影响扩散和流动。**

因此，继续只调时间步、CFL 或 PHREEQC 容差，无法把当前模型自动变成论文模型。需要修改固相表示、化学变量和沉淀入口。

---

# 一、论文中真正需要复现的模型

## 1. Zhang 2010 实验条件

主要实验域和条件是：

| 参数        |      Zhang 实验 |
| --------- | ------------: |
| 孔隙网络      |   2 cm × 1 cm |
| 微模型深度     |      17–21 μm |
| 圆柱颗粒直径    |        300 μm |
| 孔体宽度      |        180 μm |
| 孔喉宽度      |         40 μm |
| 初始孔隙率     |          0.39 |
| Darcy 流速  |   1.25 cm/min |
| CaCl₂ 入口  |  25 mM，pH 6.1 |
| Na₂CO₃ 入口 | 25 mM，pH 10.9 |
| 初始冲洗液     | DI 水，pH 约 5.7 |

两种反应液从独立入口并排进入，只通过横向扩散混合，在中心混合带产生 CaCO₃。第三个 HCl 入口位于孔隙网络之后，主要用于防止出口管线堵塞，不应直接并入首版孔隙域。

实验中出现 Vaterite 和 Calcite，但 **Vaterite 占主导**；随着过饱和度升高，Vaterite 比例增加。25 mM 实验对应 (\Omega_\text{calcite}=4.6)、(\Omega_\text{vaterite}=3.9)。

Zhang SI 中还给出了不同位置孔体的沉淀面积时间曲线，后续需要从这些图中重新高精度数字化，而不能只使用目前仓库里的近似目测值。

## 2. Yoon 2012 数值模型

Yoon 并没有用移动 level-set 表示首版沉淀，而是在每个网格单元中定义 CaCO₃ 沉淀体积分数 (V_m)：

[
\frac{\partial V_m}{\partial t}
===============================

\bar V_m s_{cc} I_m
]

显式更新为：

[
V_m^{n+1}
=========

V_m^n+\bar V_m s_{cc}I_m\Delta t
]

其中反应速率为：

[
I_m=
-\left(
k_1a_{\mathrm{H^+}}
+k_2a_{\mathrm{H_2CO_3}}
+k_3
\right)
(1-\Omega)
]

当 (\Omega>1) 时 (I_m>0)，表示沉淀；当 (\Omega<1) 时表示溶解。

Yoon 的关键实现还包括：

* 只模拟 Vaterite；
* 网格尺寸为 (5\times5\times20\ \mu\mathrm m^3)；
* (D=9.0\times10^{-10}\ \mathrm{m^2/s})；
* (D_{\mathrm{eff}}=D(1-V_m)^n)；
* Case 1 采用 (n=2)；
* (V_m>0.6) 时认为该单元不再导流，并更新流场；
* 沉淀面积定义为所有 (V_m>0.05) 单元的投影面积；
* 顶面和底面也计入反应面积，这对复现早期快速沉淀非常重要；
* Case 5 在溶解阶段使用 (df=300)，用于加快后期溶解；
* 对比时间为 13、18、118 min。

所以，**Yoon benchmark 的最忠实复现变量应当是 (V_m)，不是 level-set 界面位移。**

---

# 二、邓老师沉淀建模思路对你的启示

邓老师 2021 年的孔尺度沉淀研究非常适合用来判断当前机制是否合理。该工作明确区分了两种沉淀模式：

### Homogeneous nucleation

晶核在过饱和孔隙水中产生，随后生长。结果是沉淀带沿两股溶液的混合线形成，与 Zhang–Yoon 的中心沉淀带相似。

### Surface growth

沉淀只在已有矿物表面增长，结果通常是对颗粒表面的包覆，空间位置强烈受初始颗粒几何控制。

邓老师的模拟结果明确指出：**混合诱导沉淀带更接近 homogeneous-nucleation 情形，而 surface-growth 情形主要产生颗粒包覆。**

这意味着当前项目需要保留两个独立模式：

```text
yoon_seeded_microcontinuum
    用于严格复现 Yoon
    顶面/底面作为初始有效反应面积
    Vm 表示沉淀体积分数

deng_homogeneous_nucleation
    用于更机制化的混合诱导成核研究
    在高 SI 孔隙水中生成新晶核
    后续晶体生长更新 Vm
```

不要把它们混成一个“已有颗粒表面 Calcite level-set 生长”模型。

邓老师后续工作还说明：

* 同样的总孔隙率下降，晶核数量和空间分布不同，可导致完全不同的渗透率下降；
* 沉淀造成的 (k-\phi) 关系具有明显路径依赖；
* 临界孔隙率并不是材料常数，而取决于沉淀位置和孔隙连通性；
* 沉淀发生在主流通道、孔喉、滞流区或颗粒表面，对堵塞效果完全不同。
* 成核速率与对流速率的竞争决定沉淀更倾向于表面区域还是孔隙内部。

最新综述进一步强调，应将热力学过饱和、成核、晶体生长、输运限制和孔隙结构反馈分开建模，而不能只依据 SI 判断“有无沉淀”。

---

# 三、当前代码中的关键问题

## P0：必须先修复的问题

### 1. 当前矿物相错误

配置使用：

```matlab
cfg.phreeqcRateLaw = 'phreeqc_database_calcite';
cfg.mineralEvolutionMode = 'signed_calcite_surface';
```

同时关闭了均质和异质成核。

这不等于 Yoon 模型。严格复现应使用：

```text
mineral phase = Vaterite
reaction law  = Yoon/Chou explicit rate law
solid state   = precipitate volume fraction Vm
```

PHREEQC 可以负责水相 speciation 和活度，但不建议首版直接采用数据库中的 Calcite kinetic rate 代替 Yoon 的 Vaterite 速率律。

---

### 2. Silicon/Pyrex 基底被当成 Calcite 库存

当前代码计算：

```matlab
solidVolume = totalArea - waterArea;
calcite_moles = solidVolume / molarVolume;
```

也就是说，所有圆柱固体和后续固体几何都被当作可反应 Calcite。

但实验中的初始固体是：

```text
silicon posts + oxide layer + Pyrex cover
```

它们不是 CaCO₃，不能在欠饱和时被 PHREEQC 当作 Calcite 溶掉。

必须拆分为：

```text
substrateMask / substrateLevelSet
    固定不动的硅柱和玻璃壁

precipitateVolumeFraction Vm
    动态生成的 Vaterite/CaCO3

optional precipitateLevelSet
    后续显式表面模式才使用
```

这是当前最严重的物理错误之一。

---

### 3. 当前模型无法在孔体中央产生新沉淀

PHREEQC builder 中只有满足下列条件的单元才拥有活跃的 kinetic mineral：

```matlab
interfaceAreaCm2 > 0
waterVolumeCm3 > 0
calciteMoles > 0
```

孔体中央虽然可能高度过饱和，但没有已有 level-set 界面，因此：

```text
interfaceArea = 0
calciteMoles = 0
→ kinetics inactive
→ 不会生成首个晶核
```

这与 Zhang/Yoon 中“中心混合带生成新 CaCO₃”的过程相反。

---

### 4. 缺少顶面和底面反应面积

当前反应面积只计算：

[
A_{\mathrm{interface}}
======================

L_{\mathrm{levelset}}\times h
]

即二维截面中固液线段长度乘模型厚度。

Yoon 则在每个孔隙单元中加入：

[
A_{\mathrm{top+bottom}}=2\Delta x\Delta y
]

并在紧邻圆柱的单元中加入垂直壁面面积。缺少这个面积会严重低估早期反应，而且让孔体中央没有沉淀入口。

---

### 5. pH 10.9 被代码强制改成最高 pH 7

配置正确写入了 Na₂CO₃ 入口 pH 10.9：

```matlab
H_total = 10^(-10.9) * 1e-3;
```

但是 PHREEQC builder 默认：

```matlab
minHForPH = 1e-7;
hMolL = max(h*1000, minHForPH);
pH = -log10(hMolL);
```

这会把所有 (pH>7) 的流体强制设置为 pH 7。

因此当前 Na₂CO₃ 入口化学实际上没有进入论文所要求的强碱条件。

这不仅是数值保护过强，而是直接改变了 benchmark。

---

### 6. 输运了自由 H⁺，而不是守恒酸碱组分

当前代码把 PHREEQC 输出的 `m_H+` 写回输运场：

```matlab
hTransport.U = result.h_mol_cm3;
```



自由 H⁺ 会随着水相络合和平衡反应跨很多数量级变化，它不是适合独立输运的守恒组分。这很可能是无裁剪运行中 H runaway 和 PHREEQC 水活度不收敛的根源之一。

推荐输运：

```text
Ca_total
C_total
Na_total
Cl_total
Alkalinity
```

而不是输运自由 H⁺。pH、H⁺、HCO₃⁻、CO₃²⁻、H₂CO₃ 应由 PHREEQC 或 Yoon 平衡方程在每个反应步重新计算。

若后续改用 PhreeqcRM，则直接使用 `FindComponents()` 返回的守恒 component 向量，不要手动选自由物种。

---

### 7. 初始溶液配置实际上没有生效

配置中给了一个初始 H 浓度，但初始化代码直接写成：

```matlab
% intended initial concentration
hydrogenConcentration.setdata(0, @(t, x) 0);
```

实验开始前使用的是 DI 水，pH 约 5.7。

因此应先用 PHREEQC 生成来源明确的初始 DI 水 component state，而不是设置零 H⁺或人为预混合 pH。

---

### 8. 浓度裁剪不能作为稳定方法

当前 limiter 直接执行：

```matlab
data = max(data, 0);
data(data > upperBound) = upperBound;
```



这会无来源地增加或删除 Ca、C、Na、Cl 质量。

仓库 README 已明确记录：118 min 运行使用 `phreeqcTransportMaxFactor=2`，并持续出现：

```text
overshoot_c
advective_cfl_gt_1
mass_balance_drift
```

无裁剪运行又在约 0.336 s 出现 H runaway 和 PHREEQC water-activity failure。

所以这次 118 min 结果只能证明输出链被打通，不能证明沉淀模拟成功。

---

## P1：完成 P0 后必须修复的问题

### 9. 网格严重不足

当前诊断运行使用过 (24\times24) 网格。对 0.30 cm 域而言：

[
\Delta x = \frac{0.30\ \mathrm{cm}}{24}
=125\ \mu\mathrm m
]

而实验孔喉只有 40 μm，意味着孔喉甚至不能被一个网格单元正确解析。

Yoon 使用 5 μm 网格，即每个孔喉约 7–8 个网格。

开发阶段可先用 10 μm，但最终验证至少应完成：

```text
10 μm → 5 μm → 2.5 μm
```

的网格收敛测试。

---

### 10. 当前几何只是近似交错圆柱阵列

当前代码直接用：

```matlab
pitchX = postDiameter + poreBody;
pitchY = postDiameter + poreThroat;
```

生成交错圆柱。代码自身也注明这只是 approximate array。

首个开发 benchmark 可以继续使用它，但不能用于最终图像级验证。最终应：

1. 从 Zhang/Yoon 图像截取前三个孔体；
2. 标定 300 μm 柱径；
3. 生成二值 TIF 或 DXF；
4. 固定论文坐标系和 first-pore mask；
5. 把原始图、标定点和处理脚本纳入仓库。

---

### 11. 当前面积定义不等于论文定义

当前面积指标是：

```text
某个 x 范围内当前固体面积
−
同一区域初始固体面积
```

Yoon 的模拟沉淀面积是：

[
A_p=\sum_{V_m>0.05}\Delta x\Delta y
]

而且 first pore 和 first three pores 应使用严格几何 mask，而不是单纯的 x 坐标窗口。

因此当前面积曲线只能作为内部诊断，不能直接和 Yoon Figure 4/7 对比。

---

### 12. 参考曲线仍是近似目测数字化

仓库已经明确记录，当前 CSV 是从栅格图像进行的近似读取，并非出版商表格或高精度 WebPlotDigitizer 数据。

正式 benchmark 应保存：

```text
原始截图
坐标轴标定点
WebPlotDigitizer project
原始导出 CSV
单位转换脚本
数字化不确定度
```

---

# 四、建议的最终模型架构

## 1. 固相状态

```matlab
state.substrateMask
% 固定硅柱、玻璃边界；永不参与 CaCO3 质量库存

state.Vm
% 每个流体单元的 CaCO3/Vaterite 体积分数，0 <= Vm <= 1

state.blockedMask
% Yoon benchmark: Vm >= 0.6

state.precipitateMoles
% Vm * cellVolume / vateriteMolarVolume
```

局部流体体积分数：

[
\phi_f=(1-\chi_{\mathrm{substrate}})(1-V_m)
]

首版不要把沉淀强制转成 level-set。

## 2. 输运状态

```matlab
state.components = [
    Ca_total,
    C_total,
    Na_total,
    Cl_total,
    Alkalinity
];
```

pH 以及自由物种全部是派生量：

```matlab
chem.pH
chem.aH
chem.aCa
chem.aCO3
chem.aH2CO3
chem.OmegaVaterite
```

## 3. 反应面积

对每个孔隙单元：

[
A_{\mathrm{react}}
==================

A_{\mathrm{top+bottom}}
+
A_{\mathrm{post-wall}}
+
A_{\mathrm{precipitate}}
]

其中：

[
A_{\mathrm{top+bottom}}=2\Delta x\Delta y
]

Yoon 严格复现模式中，这个面积从 (t=0) 就存在。

Deng homogeneous-nucleation 模式中，初始孔隙内部面积可以为零，待成核生成晶核后再增加。

## 4. 反应更新

定义沉淀正的摩尔增量：

[
\Delta n_p
==========

I_mA_{\mathrm{react}}\Delta t
]

则：

[
V_m^{n+1}
=========

V_m^n+
\frac{\bar V_m\Delta n_p}{V_{\mathrm{cell}}}
]

若采用 alkalinity 作为守恒变量，沉淀时：

[
\Delta C_{\mathrm{Ca,T}}
========================

-\frac{\Delta n_p}{V_w}
]

[
\Delta C_{\mathrm{C,T}}
=======================

-\frac{\Delta n_p}{V_w}
]

[
\Delta Alk
==========

-2\frac{\Delta n_p}{V_w}
]

溶解时符号自动反向。Na、Cl 不参与反应。

## 5. 输运和流动反馈

[
D_{\mathrm{eff}}=D(1-V_m)^n
]

Yoon Case 1：

```matlab
n = 2;
blockedMask = Vm >= 0.6;
```

只要 `blockedMask` 发生改变，就重新计算流场。

后续 Deng 扩展可改成连续 Brinkman 惩罚，而不是突然阻断：

[
K(V_m)\rightarrow 0
\quad\text{as}\quad V_m\rightarrow V_{m,c}
]

---

# 五、PHREEQC 的正确角色

为了严格复现 Yoon，建议把 PHREEQC 从“决定全部动力学”改成：

> **PHREEQC 负责水相平衡、活度和 SI；MATLAB 负责论文指定的 Vaterite 动力学和固相更新。**

接口应当是：

```matlab
chem = backend.speciate(componentTotals, temperature);

rate = precip_EvaluateYoonVateriteRate( ...
    chem.aH, chem.aH2CO3, chem.aCa, chem.aCO3);

[state.components, state.Vm] = ...
    precip_ApplyVateriteReaction(state, rate, dt);
```

建议保留三种后端：

```text
yoon_equilibrium
    论文六物种平衡模型
    用作最小、快速、可重复的参考实现

iphreeqc_speciation
    PHREEQC 计算 pH、活度、SI
    与 yoon_equilibrium 做单元测试交叉验证

phreeqc_kinetics_calcite
    当前 signed KIN_DELTA 路线
    用于一般 Calcite 研究，不用于声称复现 Yoon
```

当前每个化学步都会重新创建 COM server：

```matlab
iphreeqc = actxserver('IPhreeqcCOM.Object');
```

小型 smoke 可以接受，但 5 μm 网格下性能会成为瓶颈。正确性验证后，应改为：

* 持久化 IPhreeqc 对象；
* 或使用 PhreeqcRM；
* 或为 Yoon 六物种体系建立经过 PHREEQC 验证的查找表。

---

# 六、完整可行的实施路线

## 阶段 0：冻结 benchmark 规范

新增：

```text
benchmark/
  ZhangYoonBenchmarkSpec.m
  sources.md
  geometry/
  digitized_data/
```

规范中明确记录：

* Zhang 实验值；
* Yoon Case 1–5 参数；
* 单位转换；
* 哪些值来自论文；
* 哪些是数值控制；
* 哪些是当前近似。

**验收条件：**

* 代码中不再散落来源不明的 benchmark 参数；
* 25 mM、pH、流速、扩散系数、Vaterite (K_{sp})、摩尔体积均可追溯；
* Calcite 与 Vaterite 参数不能混用。

---

## 阶段 1：无反应双入口输运 benchmark

先关闭所有反应和几何更新，仅验证：

```text
CaCl2 lower/upper inlet
Na2CO3 other inlet
steady Stokes flow
Ca/C/Na/Cl/Alk transport
```

建议新增：

```text
precip_RunPassiveSplitInletBenchmark.m
precip_ComputeComponentMassLedger.m
precip_TestMixingWidth.m
```

**必须停止使用浓度裁剪。**

若输运产生负值或超调，应：

1. 拒绝当前时间步；
2. 缩小 transport substep；
3. 重算；
4. 记录 rejected-step 次数。

**验收条件：**

* Ca/C/Na/Cl 均非负；
* Na、Cl 全局质量严格守恒；
* 累积相对质量误差建议低于 (10^{-4})；
* mixing band 关于中心线基本对称；
* 10 μm 与 5 μm 的 mixing-width 差异小于约 5%。

---

## 阶段 2：单单元和零维混合化学

构造混合比例：

[
f=0,0.05,\dots,1
]

其中：

[
C(f)=fC_{\mathrm{CaCl_2}}+(1-f)C_{\mathrm{Na_2CO_3}}
]

对每个 (f)：

* PHREEQC 计算 pH；
* 计算 Vaterite SI；
* 输出 (a_{\mathrm{Ca^{2+}}})、(a_{\mathrm{CO_3^{2-}}})、(a_{\mathrm{H^+}})、(a_{\mathrm{H_2CO_3}})；
* 与 Yoon 平衡模型交叉验证。

**验收条件：**

* 两入口 pH 分别保持约 6.1 和 10.9；
* 不再有 pH 7 截断；
* Ca、C、Na、Cl 和电荷平衡闭合；
* SI 峰值出现在中间混合比例附近；
* 无 `A(H2O) Activity of water has not converged`。

---

## 阶段 3：固定流场、固定几何的 Yoon Case 1

新增核心文件：

```text
precip_YoonMicrocontinuumSolver.m
precip_ComputeYoonReactiveArea.m
precip_EvaluateYoonVateriteRate.m
precip_UpdateVateriteVolumeFraction.m
precip_UpdateYoonEffectiveDiffusivity.m
```

此阶段：

* 流场固定；
* 使用 (V_m)；
* 加入顶底面面积；
* 使用 Vaterite 速率；
* 先不做堵塞后的流场更新。

**验收条件：**

* 沉淀首先出现在中心混合带；
* Ca 和 C 按 1:1 被消耗；
* Alk 按 2 equivalents/mol 下降；
* 固相增量和水相损失逐步守恒；
* 13 min 已形成明显中心沉淀带。

---

## 阶段 4：扩散反馈敏感性

完成：

```text
Case 2: n = 0
Case 1: n = 2
Case 3: n = 3
```

[
D_{\mathrm{eff}}=D(1-V_m)^n
]

**验收条件：**

结果应出现 Yoon 报告的趋势：

* (n=0)：堵塞对扩散抑制最弱，沉淀更多；
* (n=3)：扩散下降最快，沉淀更受限；
* (n=2)：作为 base case。

这一步可以验证扩散—沉淀反馈是否真正接入，而不只是输出一个 (D_{\mathrm{eff}}) 数值。

---

## 阶段 5：流动堵塞反馈和 Case 1 完整复现

加入：

```matlab
blockedMask = Vm >= 0.6;
```

只要新单元被堵塞，就更新 Stokes 流场。

输出：

* 13、18、118 min (V_m) 场；
* (V_m>0.05) 沉淀面积；
* first pore 面积；
* first three pores 面积；
* 流速重分配；
* 压降、流量和相对渗透率。

**验收条件：**

* 沉淀区域与中心混合带一致；
* 13 和 18 min 形态接近 Yoon Case 1；
* 模拟能复现早期增长和最大面积量级；
* 允许 Case 1 无法复现实验后期快速面积下降，因为这正是论文结论。

---

## 阶段 6：Yoon Case 5

只在欠饱和溶解时应用：

```matlab
dissolutionFactor = 300;
```

不能把它应用于沉淀阶段。

输出 Case 1 与 Case 5 并排比较。

**验收条件：**

* Case 5 在约 15–30 min 出现显著面积下降；
* Case 1 后期下降较弱；
* 不应把 `df=300` 宣称为独立实验测得参数，它是 Yoon 用于解释后期快速溶解的有效放大参数；
* 118 min 仍可能高于 Zhang 实验，这也是 Yoon 原文指出的模型缺口。

---

## 阶段 7：邓老师机制扩展

在完成 Yoon benchmark 后再增加：

```text
precipitationMode = yoon_seeded_surface
precipitationMode = deng_homogeneous_nucleation
precipitationMode = heterogeneous_nucleation
precipitationMode = levelset_surface_growth
```

Deng homogeneous-nucleation 模式需要独立状态：

```matlab
nucleiNumberDensity
crystalliteMoles
precipitateSurfaceArea
precipitateVolumeFraction
nucleationEventCount
```

应先做确定性有效成核模型，再做随机成核。不要一开始直接加入完整 CNT，因为参数不可辨识性很强。

随后研究：

* 晶核数量与位置对 (k-\phi) 的影响；
* critical porosity；
* critical diffusivity；
* 沉淀带宽度；
* 孔喉优先堵塞；
* stochastic realizations。

---

## 阶段 8：显式几何与 level-set

level-set 应作为第二层模型，而不是 Yoon benchmark 的第一入口。

推荐触发方式：

```text
Vm < threshold
    保持 micro-continuum 体积分数

Vm 接近 1 且形成连通固体
    转换为 precipitateLevelSet

substrateLevelSet
    始终独立且固定
```

转换时必须保守：

[
\Delta V_{\mathrm{levelset}}
============================

\sum V_mV_{\mathrm{cell}}
]

否则几何重构会凭空产生或损失矿物质量。

现有 signed rate helper：

```matlab
rate = -deltaMoles/(A*dt)
```

符号方向本身是合理的，可以在这个阶段复用。

---

# 七、建议重组代码，而不是继续扩展 4800 行主文件

保留：

```text
precip_PNM_beauty3.m
```

作为已有 level-set/Calcite smoke 路径。

新建一个专门的 Yoon 求解器：

```text
ReactiveTransport/RTM/precipitate/
  run_zhang_yoon_caco3_precipitation_benchmark.m

  benchmark/
    precip_ZhangYoonBenchmarkSpec.m
    precip_LoadZhangYoonGeometry.m
    precip_LoadReferenceCurves.m

  solver/
    precip_YoonMicrocontinuumSolver.m
    precip_AdvanceTransport.m
    precip_AdvanceReaction.m
    precip_UpdateFlowMask.m

  chemistry/
    precip_ChemistryBackend.m
    precip_IPhreeqcSpeciation.m
    precip_YoonCarbonateEquilibrium.m
    precip_YoonVateriteRate.m

  precipitation/
    precip_ComputeYoonReactiveArea.m
    precip_UpdateVateriteVolumeFraction.m
    precip_UpdateEffectiveDiffusivity.m

  diagnostics/
    precip_ComputeElementMassLedger.m
    precip_ComputeYoonAreaMetrics.m
    precip_ExportYoonSnapshots.m

  tests/
    test_inlet_speciation.m
    test_passive_split_inlet_mass.m
    test_vaterite_rate_sign.m
    test_reactive_area.m
    test_single_cell_precipitation.m
    test_case1_short.m
    test_case5_short.m
```

---

# 八、推荐时间推进主循环

```matlab
initializeStaticSubstrate();
initializeConservativeComponents();
Vm = zeros(numCells, 1);

while time < endTime

    % 1. 根据 substrate + blocked Vm 求流场
    if flowTopologyChanged
        flow = solveStokes(substrateMask, Vm >= 0.6);
    end

    % 2. 保守输运，可在一个反应步中多次 subcycle
    componentsTrial = transportComponents( ...
        components, flow, Deff, dtTransport);

    % 3. PHREEQC 或 Yoon 平衡模型做 speciation
    chem = chemistryBackend.speciate(componentsTrial);

    % 4. 根据论文速率律计算 signed Vaterite rate
    Im = evaluateYoonVateriteRate(chem);

    % 5. 选择反应时间步，限制单步 Vm 变化
    dtReaction = limitByPrecipitateFraction(Im, Areact, Vm);

    % 6. 更新水相守恒组分和 Vm
    [componentsTrial, VmTrial] = ...
        applyVateriteReaction(componentsTrial, Vm, Im, dtReaction);

    % 7. 质量、非负性和范围审核
    audit = computeMassLedger(...);

    if ~audit.accepted
        reduceTimeStep();
        continue
    end

    % 8. 接受时间步
    components = componentsTrial;
    Vm = VmTrial;
    Deff = D .* (1 - Vm).^n;

    exportIfDue();
end
```

建议反应时间步满足：

[
\max|\Delta V_m|\le 0.01\sim0.02
]

Yoon 初期最大步长约 0.1 min，后期逐步增至 2 min；但你的 transport substep 仍应由网格速度、扩散和精度要求单独控制，不能直接用一个 60 s 宏步同时完成全部输运和反应。

---

# 九、最终验收矩阵

| 层次                 | 必须通过的检查                               |
| ------------------ | ------------------------------------- |
| Chemistry          | 入口 pH 6.1/10.9；Vaterite SI 正确；无 pH 截断 |
| Passive transport  | 无负浓度；无裁剪；各 component 质量守恒             |
| Reaction           | Ca/C/Alk 与固相质量严格对应                    |
| Morphology         | 沉淀首先出现在中心 mixing band                 |
| Area               | 使用 (V_m>0.05) 及严格区域 mask              |
| Diffusion feedback | Case 2/1/3 趋势正确                       |
| Flow feedback      | (V_m>0.6) 后流场发生重分配                    |
| Case 1             | 早期增长与最大面积合理                           |
| Case 5             | 后期下降显著快于 Case 1                       |
| Grid               | 至少完成 10、5 μm 收敛，正式结果采用 5 μm           |
| Benchmark claim    | 不使用 finite clipping 的结果进行定量声明         |
| Reproducibility    | 几何、数字化数据、数据库、参数和代码提交全部锁定              |

---

# 最终建议

当前应当：

**保留** split inlet、signed reaction、测试、日志和输出框架。

**暂停**使用当前 `signed_calcite_surface + level-set` 结果声称复现 Zhang/Yoon。

**优先重写**以下四个核心部分：

1. 静态 substrate 与动态 CaCO₃ 固相分离；
2. 自由 H⁺ 输运改为守恒 component 输运；
3. 建立 Vaterite (V_m) micro-continuum 模型；
4. 加入顶底面反应面积、(D_{\mathrm{eff}}) 和 (V_m=0.6) 堵塞反馈。

第一项实际开发任务应是建立一个不调用 level-set 的 `precip_YoonMicrocontinuumSolver.m`，先完成 **无反应双入口守恒输运 + 单单元 PHREEQC speciation**。在这两项通过前，不应继续运行 118 min 全耦合算例。

本次审核基于仓库当前分支代码、执行计划和记录的运行结果；我没有在本环境中实际启动你的 MATLAB、IPhreeqcCOM 和完整 118 min 计算。
