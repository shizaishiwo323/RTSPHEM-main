# 审核结论

这轮改动已经完成了**关键路线纠偏**：你不再只把旧的 Calcite–level-set 溶解代码反向使用，而是新增了独立的 **Yoon–Vaterite–(V_m) micro-continuum** 路径，区分静态基底和动态沉淀，输运变量也改成了 `Ca_total、C_total、Na_total、Cl_total、Alkalinity`。这一方向明显更接近 Yoon 2012，也符合邓老师“局部沉淀形态—扩散率—流动性质反馈”的建模思路。

当前状态可以概括为：

> **模型骨架基本正确，测试和证据链做得很好；但有限体积输运、反应面积、物质库存和流场反馈还没有形成一个真正闭合的定量求解器。**

因此现在不是推翻重做，而是要把现有 scaffold 中几处“诊断实现”替换成真正参与计算的物理实现。

---

# 一、对当前改动的分项评价

| 模块                       | 当前状态                                          | 审核判断                              |
| ------------------------ | --------------------------------------------- | --------------------------------- |
| Yoon 模型路线                | 已增加 `Vaterite + Vm + conservative components` | **方向正确**                          |
| 静态基底与沉淀分离                | `substrateMask` 与 `Vm` 分开                     | **关键问题已修正**                       |
| PHREEQC 定位               | 逐渐转向只负责水相 speciation                          | **正确**                            |
| Signed Calcite level-set | 仍作为旧路径存在                                      | 可保留作 surface-growth 诊断，但不能代表 Yoon |
| 无裁剪输运                    | 已有 FV、CFL、拒绝/缩步框架                             | **框架正确，实际守恒仍有缺口**                 |
| 沉淀反应                     | 已有 Vaterite 速率、(V_m)、(D_\mathrm{eff})         | **部分完成**                          |
| 反应面积                     | 目前主要是顶面和底面                                    | **不完整**                           |
| 流动反馈                     | 有 proxy、Darcy、诊断 Stokes–Brinkman              | **尚未生产验证**                        |
| Case 1/Case 5            | 已有比较框架和 manifest                              | **尚非论文复现**                        |
| 几何、区域 mask、数字化数据         | package 和 gate 已搭好                            | **资产尚未补齐**                        |
| 定量审核 gate                | 非常完整且保守                                       | **工程上做得很好**                       |

这和邓老师的主要思路是一致的：沉淀不能只转化为“总孔隙率减少”，而要显式考虑沉淀发生位置、成核或表面生长方式，以及由此产生的临界扩散率和堵塞路径。邓老师 2021 年的工作还显示，homogeneous nucleation 和 surface growth 会形成不同沉淀形态和不同的临界孔隙率；2024 年工作进一步说明，即使孔隙率损失相同，不同晶核分布也会产生不同渗透率下降。

---

# 二、当前最需要修正的问题

## P0-1：用户入口仍然调用旧的 Calcite–level-set 路径

目前公开的：

```matlab
run_zhang_yoon_caco3_precipitation_benchmark()
```

仍然执行：

```matlab
cfg = precip_ConfigureZhangYoonBenchmark(overrides);
result = precip_PNM_beauty3(cfg);
```

也就是旧的 signed-Calcite level-set 求解器，而不是新建的 Yoon (V_m) 求解器。

README 又把它称为“Run the local Zhang/Yoon benchmark”，而新的 Yoon 路径被称为 foundation smoke 和 diagnostic runner。

### 应立即调整为

```text
run_zhang_yoon_caco3_precipitation_benchmark.m
    → 正式调用 precip_RunYoonFixedGeometryBenchmark

run_legacy_signed_calcite_levelset_diagnostic.m
    → 保留当前 precip_PNM_beauty3 路径
```

新入口建议：

```matlab
function result = run_zhang_yoon_caco3_precipitation_benchmark(overrides)

if nargin < 1
    overrides = struct();
end

specOverrides = getFieldOrDefault(overrides, 'spec', struct());
runOptions   = getFieldOrDefault(overrides, 'options', struct());

spec = precip_ZhangYoonBenchmarkSpec(specOverrides);
result = precip_RunYoonFixedGeometryBenchmark(spec, runOptions);
end
```

每个结果 manifest 必须写入：

```text
modelFamily = yoon_seeded_microcontinuum
mineralPhase = Vaterite
solidStateVariable = Vm
legacyLevelSetPathUsed = false
```

---

## P0-2：当前有限体积输运没有真正识别基底和沉淀结构

`precip_AdvanceConservativeTransport2D` 目前：

* 使用全矩形域上的标量扩散系数；
* 没有接收 `substrateMask`；
* 没有使用 `fluidVolumeFraction`；
* 没有把 `effectiveDiffusivity_cm2_s` 传入面通量；
* 溶质会数值上穿过圆柱基底；
* 被 (V_m\ge0.6) 阻断的单元，其对流和扩散没有被分别处理。

虽然代码已经计算：

[
D_\mathrm{eff}=D(1-V_m)^n
]

但 finite-volume transport 实际仍使用 `spec.diffusionCoefficient_cm2_s`。

更严重的是，扩散反馈敏感性中：

```matlab
feedbackCoupled = true(3,1);
```

是直接赋值，而不是由“输运通量确实使用了 (D_\mathrm{eff})”推导得到。

### 正确的 micro-continuum 输运状态

建议把真正守恒变量改成单元库存：

[
N_i=\theta C_i V_{\mathrm{cell}}
]

其中：

[
\theta=(1-\chi_\mathrm{substrate})(1-V_m)
]

每个面的总摩尔通量：

[
F_{i,f}
=

## u_f C_{i,\mathrm{upwind}}A_f

D_{i,f}
\frac{C_{i,R}-C_{i,L}}{d_f}A_f
]

更新：

[
N_i^{n+1}
=========

N_i^n-\Delta t\sum_f F_{i,f}+\Delta N_{i,\mathrm{reaction}}
]

再恢复水相浓度：

[
C_i^{n+1}
=========

\frac{N_i^{n+1}}{\theta^{n+1}V_{\mathrm{cell}}}
]

其中必须区分：

* `substrateMask`：对流和扩散均为零；
* `blockedMask, Vm >= 0.6`：**只关闭流动对流**；
* 阻断单元内扩散仍按 (D(1-V_m)^n) 保留；
* 当 (V_m\to1) 时 (D_\mathrm{eff}\to0)。

新函数建议命名：

```text
solver/precip_AdvanceMicrocontinuumTransport2D.m
solver/precip_AssembleComponentFaceFluxes.m
solver/precip_ComputeMicrocontinuumStableDt.m
```

---

## P0-3：当前 passive transport benchmark 可以“空跑通过”

`precip_RunPassiveSplitInletBenchmark` 默认：

1. 直接构造解析 mixing profile；
2. 默认 initial source 就是这个目标 profile；
3. analytic candidate 再次返回同一个 profile；
4. 最后质量账本调用：

```matlab
precip_ComputeComponentMassLedger(components, components, spec)
```

前后对象完全相同，因此质量误差必定为零。

这只能证明接口和 manifest 能运行，不能证明有限体积输运守恒。

### 应改成真正的测试

初始状态：

```text
整个孔隙域 = DI water
t = 0 开始 split inlet
reaction = off
```

连续推进多个真实 FV 子步，累计：

[
\epsilon_i=
\frac{
N_i(t)-N_i(0)
-\int_0^t(F_{i,\mathrm{in}}-F_{i,\mathrm{out}}),dt
}{
\max(N_i(0),N_{i,\mathrm{in}},\epsilon)
}
]

通过条件建议：

```text
Ca/C/Na/Cl minimum >= -1e-14 mol/cm3
Na relative boundary-closure error <= 1e-8
Cl relative boundary-closure error <= 1e-8
no clipping
no transport reset
rejected substeps reported
mixing profile symmetric within 2–5%
zero leakage through substrate
```

必须增加以下测试：

```text
test_yoon_transport_closed_box_mass.m
test_yoon_transport_open_boundary_mass.m
test_yoon_transport_impermeable_post.m
test_yoon_transport_deff_barrier.m
test_yoon_transport_split_inlet_symmetry.m
```

---

## P0-4：水相浓度、bulk inventory 和 (V_m) 的体积基准仍不一致

当前反应更新使用：

```matlab
deltaConcentration = acceptedDeltaMoles / cellVolume
```

并将其直接从水相浓度中减去。

但当 (V_m) 增加时，水体积应从：

[
V_w=V_\mathrm{cell}
]

变成：

[
V_w=(1-V_m)V_\mathrm{cell}
]

当前质量账本仍然使用：

```matlab
sum(concentration) * cellVolume
```

没有乘流体体积分数。

### 建议明确唯一约定

最稳妥的是：

```text
state.componentMoles.<name>       单元内摩尔数，主状态
state.aqueousConcentration.<name> 水相浓度，仅供化学和输出
```

反应后：

```matlab
state.componentMoles.Ca_total = ...
    state.componentMoles.Ca_total - deltaPrecipitateMoles;

state.componentMoles.C_total = ...
    state.componentMoles.C_total - deltaPrecipitateMoles;

state.componentMoles.Alkalinity = ...
    state.componentMoles.Alkalinity - 2*deltaPrecipitateMoles;

waterVolume = max(theta .* spec.cellVolume_cm3, minWaterVolume);
state.aqueousConcentration = state.componentMoles ./ waterVolume;
```

这样输运、反应、PHREEQC 和质量审核使用同一个库存。

---

## P0-5：不能用“Alkalinity 必须非负”限制沉淀

当前代码用：

```matlab
alkAvailable = max(Alkalinity,0) * V / 2;
maxPrecipitation = min(Ca, C, Alk/2);
```

限制沉淀量。

但 alkalinity 是带符号的守恒量，不是必须非负的元素库存。负 alkalinity 在酸性水中完全允许。

PHREEQC 输入中也存在：

```matlab
Alkalinity max(samples.Alkalinity,0)
```

这会把 CaCl₂ 入口 pH 6.1 对应的负 alkalinity 直接改成零。

### 修正原则

沉淀最大量只按以下库存限制：

[
\Delta n_p
\le
\min(N_{\mathrm{Ca}},N_{\mathrm{C}})
]

随后按化学计量更新 alkalinity：

[
\Delta Alk=-2\Delta n_p
]

不要再对 alkalinity 做非负裁剪。

同时，PHREEQC 中：

```matlab
sprintf('Alkalinity %.15g', samples.Alkalinity(i)*1000)
```

必须保留原始正负号。

---

## P0-6：Vaterite 参数仍是 smoke 参数，不是 Yoon 参数

当前 benchmark spec 中：

```matlab
k1 = 0
k2 = 0
k3 = 1e-12
source = smoke_default_pending_literature_lockdown
```

Yoon/Chou 给出的参数为：

[
k_1=8.9\times10^{-1}
]

[
k_2=5.01\times10^{-4}
]

[
k_3=6.6\times10^{-7}
]

单位为 (\mathrm{mol,m^{-2},s^{-1}})。转换到当前厘米单位应是：

```matlab
k1 = 8.9e-5;   % mol cm^-2 s^-1
k2 = 5.01e-8;
k3 = 6.6e-11;
```

Yoon 的 Vaterite 参数还包括：

```matlab
Ksp_mol2_m6 = 1.832e-2;
Ksp_mol2_L2 = 1.832e-8;
molarVolume_cm3_mol = 37.47;
```

而当前 `10^(-7.91)` 并不等于 Yoon 表中的单位转换结果。Yoon 还采用扩展 Debye–Hückel 活度系数，而当前本地 chemistry backend 将总 Ca 直接当成 Ca 活度，属于 smoke 近似。

spec 应更新为：

```matlab
spec.yoonRate.k1 = 8.9e-5;
spec.yoonRate.k2 = 5.01e-8;
spec.yoonRate.k3 = 6.6e-11;
spec.yoonRate.units = 'mol_cm-2_s-1';
spec.yoonRate.sourceDoi = '10.1029/2011WR011192';
spec.yoonRate.sourceEquation = 'Yoon2012 Eq.7; Chou1989';
spec.yoonRate.mineralPhase = 'Vaterite';
spec.yoonRate.sourceValuesVerified = true;

spec.vateriteKsp_mol2_L2 = 1.832e-8;
spec.vateriteMolarVolume_cm3_mol = 37.47;
```

---

## P0-7：反应面积仍缺少两个关键部分

目前：

```matlab
area = 2 * dx * dy
```

只表示顶面和底面。

Yoon 模型还包括：

1. 与圆柱颗粒相邻单元的竖直壁面；
2. 当某单元 (V_m=1) 后，该沉淀单元暴露给相邻流体单元的竖直面；
3. 这些面在过饱和时用于继续沉淀，在欠饱和时用于溶解。

Yoon 明确指出，加入顶面、底面和圆柱壁面是复现早期快速沉淀所必需的。

### 建议的面面积算法

对每个流体单元：

[
A_\mathrm{react}
================

2\Delta x\Delta y
+
A_\mathrm{substrate-wall}
+
A_\mathrm{precipitate-face}
]

其中：

```matlab
A_substrate_wall =
    depth * sum(length of faces adjacent to substrateMask);

filledMask = Vm >= 1 - vmTolerance;

A_precipitate_face =
    depth * sum(length of faces adjacent to filledMask);
```

需要防止相邻两个满沉淀单元的内部面被重复计数。

测试几何至少包括：

```text
单个流体单元：A = 2 dx dy
邻接一个圆柱壁面：A = 2 dx dy + dy*depth
邻接两个壁面：相应叠加
一个 Vm=1 沉淀单元：只计算暴露外表面
相邻两个 Vm=1 单元：内部面不计
```

---

# 三、其他必须修正但可排在第二批的问题

## 1. 当前 benchmark 默认仍是解析 mixing profile

`precip_RunYoonCase1Short` 默认：

```matlab
transportMode = 'analytic';
reactionSubcycling = false;
```

这适合零维和反应 smoke，但不适合作为论文复现。定量 runner 必须显式要求：

```matlab
transportMode = 'finite_volume';
reactionSubcycling = true;
```

并在 quantitative mode 下禁止 analytic transport。

---

## 2. Yoon 采用准稳态反应输运，而当前是单次 transient splitting

Yoon 在每个固相更新步中：

* 当前几何下重新求稳态 Stokes；
* 求稳态多组分输运和水相平衡；
* 通过 sequential iterative approach 处理反应；
* 最后显式更新 (V_m)。

当前代码则是一次 transport、一次 reaction、一次 (V_m) 更新。

建议增加外层迭代：

```text
固定 Vm 和流场
    ↓
transport/speciation/reaction source iteration
    ↓
concentration residual < tolerance
    ↓
更新 Vm
```

首版可以实现为 SIA：

```matlab
for k = 1:maxChemTransportIterations
    componentsNew = solveSteadyTransport(components, reactionSource);
    chem = speciate(componentsNew);
    reactionSourceNew = buildReactionSource(chem);

    if residual < tolerance
        break
    end

    components = relax*componentsNew + (1-relax)*components;
end
```

定量建议：

```text
max concentration relative residual < 1e-8
max reaction-rate relative residual < 1e-6
max iterations = 50
under-relaxation = 0.5–0.8
```

---

## 3. Case 5 的 300 倍因子需要状态触发

当前 `dissolutionFactor=300` 从模拟开始就存在，只要局部速率为负就生效。

但 Yoon 的处理是：中心混合带沉淀堵塞之后，再对后续溶解施加该有效放大系数。Case 5 的 (df=300) 是反应常数和反应面积未区分机制的 lumped parameter。

建议加入：

```matlab
state.case5BoostActive = false;
state.case5ActivationTime_s = NaN;

if ~state.case5BoostActive && ...
        any(state.blockedMask(spec.centerBandMask))
    state.case5BoostActive = true;
    state.case5ActivationTime_s = currentTime;
end

if state.case5BoostActive && rate < 0
    rate = 300 * rate;
end
```

manifest 中必须输出激活时间。

---

## 4. 当前 Stokes–Brinkman 仍应视为诊断求解器

代码本身已经正确标注为：

> production benchmark claims still require grid convergence and a validated Stokes backend.

目前还有两个概念问题：

* 流体单元默认设置了 `brinkmanDrag=1 s^-1`，不是真正的自由流 Stokes；
* 入口采用固定速度，却又通过出口流量比计算相对渗透率，边界条件和 (k/k_0) 定义不统一。

建议不要继续大幅修补这个诊断求解器，而是增加：

```text
solver/precip_SolveYoonHyPHMStokes.m
```

将：

```text
solidMask = substrateMask | blockedMask
```

转换为 HyPHM level-set 或固体边界，再复用已有 `StokesLEVEL`。

边界条件应只选一种：

```text
固定压差：
    保持 Δp 不变
    k/k0 = Q/Q0

固定流量：
    保持 Q 不变
    k/k0 = Δp0/Δp
```

对照 Yoon 更适合先标定初始压差，使初始 Darcy 流速为 1.25 cm/min，然后在堵塞过程中保持该压差。

---

## 5. 当前 60×60 网格只有 50 μm

当前：

```text
L = 0.30 cm
numX = 60
dx = 50 μm
```

实验孔喉约 40 μm，因此当前一个孔喉甚至不足一个网格。

Yoon 正式结果使用 5 μm 网格，即：

* 圆柱直径约 60 个网格；
* 孔喉约 7 个网格；
* 1 μm first-pore 检查与 5 μm 的沉淀质量差异小于约 3%。

建议顺序：

```text
20 μm：开发和快速检查
10 μm：预生产
5 μm：正式 Yoon benchmark
2.5 μm：收敛检查
1 μm：只做 first-pore 局部复核
```

0.30 cm 域在 5 μm 下是 600×600 单元，当前 MATLAB 直接稀疏 `\` 求解百万级 Stokes 鞍点系统会很吃力，因此在正式网格之前必须先解决可扩展流场求解器。

---

# 四、建议的具体实施顺序

## Milestone 0：统一正式入口和文档

修改：

```text
run_zhang_yoon_caco3_precipitation_benchmark.m
README.md
Zhang_Yoon_CaCO3_precipitation_execution_plan.md
```

当前 execution plan 的顶层架构仍写成“signed PHREEQC Calcite + level-set”，与新 Yoon (V_m) 路径冲突。

应把计划拆成：

```text
A. legacy_signed_calcite_levelset_diagnostic
B. yoon_vaterite_microcontinuum_benchmark
```

验收：

```text
正式 runner 不再引用 precip_PNM_beauty3
正式 manifest.modelFamily == yoon_seeded_microcontinuum
旧路径仍可单独运行
```

---

## Milestone 1：重做守恒 FV 输运核心

修改或新增：

```text
precip_AdvanceMicrocontinuumTransport2D.m
precip_AssembleComponentFaceFluxes.m
precip_ComputeMicrocontinuumStableDt.m
precip_ComputeOpenBoundaryMassLedger.m
precip_RunPassiveSplitInletBenchmark.m
```

完成：

* substrate 内不输运；
* blocked 单元无对流但保留受限扩散；
* 使用局部 (D_\mathrm{eff})；
* 使用单元摩尔库存；
* 入口/出口面通量进入质量账本；
* 禁止 concentration clipping；
* 真正执行多个时间步。

完成后再把 quantitative runner 的 transport 默认切换为 FV。

---

## Milestone 2：锁定化学和反应

修改：

```text
precip_ZhangYoonBenchmarkSpec.m
precip_YoonCarbonateEquilibrium.m
precip_IPhreeqcSpeciation.m
precip_YoonVateriteRate.m
precip_UpdateVateriteVolumeFraction.m
precip_ComputeYoonReactionMassLedger.m
```

完成：

* 锁定 Yoon/Chou 参数和单位；
* 移除 alkalinity 非负裁剪；
* 反应库存改用 cell moles；
* PHREEQC 作为定量 speciation；
* 本地 Yoon backend 增加扩展 Debye–Hückel，或明确只作为 smoke；
* PHREEQC 与本地 backend 在 101 个 mixing fractions 上比较；
* 反应账本只比较“反应前—反应后”，不能跨过开放边界输运阶段。

验收：

```text
入口 pH 6.1 和 10.9 误差 < 0.02
Vaterite SI 峰值位于内部 mixing fraction
Ca+C+solid 守恒误差 < 1e-10
Alk+2*solid 守恒误差 < 1e-10
Na、Cl 在反应步完全不变
```

---

## Milestone 3：补齐反应面积和 Case 5

修改：

```text
precip_ComputeYoonReactiveArea.m
precip_UpdateVateriteVolumeFraction.m
precip_RunYoonCase1Case5FixedGeometryComparison.m
```

实现：

* top/bottom area；
* cylindrical-post wall area；
* (V_m=1) 后动态竖直沉淀表面；
* Case 5 中心线堵塞后激活；
* 反应面积随 (V_m) 和邻接关系更新。

---

## Milestone 4：接入生产流场

新增：

```text
precip_SolveYoonHyPHMStokes.m
precip_MapCellMaskToLevelSet.m
precip_MapHyPHMFluxToCartesianFaces.m
```

验证案例：

```text
空通道 Poiseuille
单圆柱绕流
一列堵塞单元
入口/出口流量闭合
固定压差下 k/k0
```

通过条件：

```text
divergence residual < 1e-8
Qin-Qout relative error < 1e-8
linear residual < 1e-8
新增堵塞后流场确实重算
```

---

## Milestone 5：几何与参考数据正式化

当前几何仍是 approximate cylindrical-post array，区域 mask 也可能退化为 x-window。

建立：

```text
benchmark_assets/
  geometry/
    source_image.png
    calibration.csv
    substrate_mask.mat
    first_pore_mask.mat
    first_three_pores_mask.mat
    geometry_manifest.json

  reference_digitization/
    zhang_S3b.png
    yoon_Fig4a.png
    yoon_Fig7a.png
    *.tar 或 WebPlotDigitizer project
    calibration_points.csv
    raw_export.csv
    converted_reference.csv
    uncertainty.csv
```

Zhang SI 第 4 页的 Figure S3 是 selected pore-space 曲线的主要来源，必须保存原图、标定点和数字化工程，而不是只保存手抄 CSV。

---

## Milestone 6：正式 benchmark 阶梯

按以下顺序运行，任何一级不通过都不进入下一级：

```text
B0：无反应 split-inlet mixing
B1：零维 PHREEQC/Yoon chemistry
B2：固定 Vm 的稳态反应输运
B3：Case 1，n = 2，固定流场
B4：Case 2/1/3，n = 0/2/3
B5：Vm >= 0.6 流场更新
B6：Case 5，df = 300 延迟激活
B7：10/5/2.5 μm 网格收敛
B8：13/18/118 min 图像和面积对比
```

最终 quantitative gate 至少应要求：

```text
无 finite concentration limiter
所有 component boundary mass closure 通过
反应 Ca/C/Alk/solid 守恒通过
正式 Stokes 通过
精确 geometry package 通过
精确 region masks 通过
Yoon rate constants locked
PHREEQC chemistry accepted
Case 1/Case 5 accepted
grid convergence accepted
13/18/118 min snapshots complete
isQuantitativeBenchmark = true
```

你现在的 readiness audit 设计得非常好，已经包含这些主要门槛。 但下一步应让 gate 读取**数值证据自动推导状态**，而不是接受手工布尔字段。例如：

```text
feedbackCoupled
flowBackendProductionValidated
productionComparisonValidated
```

都应由独立验证 manifest、代码 commit SHA 和输出 hash 推导。

---

# 五、邓老师思路应放在什么位置

Yoon benchmark 完成前，不建议立刻把随机 homogeneous nucleation 塞进基础模型。正确分层是：

```text
模式 1：yoon_seeded_microcontinuum
    顶底面和壁面提供初始反应面积
    用于严格复现 Yoon

模式 2：deng_homogeneous_nucleation
    过饱和孔隙水中生成新晶核
    用于解释 mixing-induced 中心沉淀带

模式 3：surface_growth
    只在已有同类矿物表面生长
    可复用原 signed level-set 思路
```

邓老师 2021 年结果显示 homogeneous nucleation 更容易复现 mixing-induced precipitation pattern，而 surface growth 更接近基底包覆；2025 年工作又表明成核速率与对流速率之比会控制沉淀发生在表面还是内部区域。

完成 Yoon 后，再增加：

```text
nucleiNumberDensity
crystalMoles
crystalSurfaceArea
nucleationEventCount
randomSeed
```

并对每个参数组运行多个随机 realization。不要再用唯一的 (k(\phi)) 曲线代表所有沉淀结果，因为邓老师相关研究已经说明，晶核数量和分布会导致明显不同的堵塞和渗透率轨迹。

---

# 建议的下三个提交

1. **`refactor: make Yoon Vm solver the authoritative Zhang-Yoon entrypoint`**
   修 runner、README、execution plan，旧 level-set 路径改名。

2. **`fix: implement masked conservative microcontinuum transport and real passive audit`**
   修 substrate 穿透、(D_\mathrm{eff})、fluid-volume inventory、开放边界质量账本和伪通过测试。

3. **`fix: lock Yoon Vaterite chemistry, kinetics, and evolving reactive area`**
   锁定 (k_1,k_2,k_3,K_{sp},\bar V_m)，修 alkalinity、竖直反应面积和 Case 5 激活。

此外，当前分支还混入了 `.pytest_cache` 和大量 `Data/T2single-RI` 生成结果，建议在进入正式 PR 前移除这些生成文件并加入 `.gitignore`，把沉淀改动整理成独立可审查的提交序列。

本次结论来自当前 GitHub 分支的静态代码审核和论文对照；没有在此环境启动 MATLAB、HyPHM 或 IPhreeqcCOM，因此所有数值通过状态仍应以修正后的真实 benchmark manifest 为准。
