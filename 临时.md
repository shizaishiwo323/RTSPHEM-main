我梳理完了。这个补充材料对我们当前脚本非常有用，尤其是作为 **Molins Part II moving-boundary benchmark 的外部参考曲线库**。

**补充材料结构**
根目录是：

[SupplementaryMaterial](</mnt/c/Users/imgw/Downloads/10596_2019_9903_MOESM1_ESM/SupplementaryMaterial>)

主要三层：

1. `InputsCodes/`
   存放五套代码的输入文件、版本/可用性说明：
   `Chombo-Crunch`、`dissolFoam`、`LatticeBoltzmann`、`OpenFOAM-DBS`、`Vortex`，以及 `_Part III-MaskImage`。

2. `Raw/`
   存放各代码原始输出，格式包括 `vtk`、`hdf5`、`xlsx`、`lpk`、`ods`、ASCII 等。

3. `Results/`
   存放转换后的 CSV / VTK / VTI / notebook / VisIt session。这里是最适合我们拿来对照的部分。

规模上大概是：

```text
InputsCodes   219 files, ~3076 MB
Raw           429 files, ~858 MB
Results       931 files, ~2888 MB
```

`Results/` 里按 benchmark 部分组织：

```text
Results/
  Part-I/
  Part-II/
  Part-III/
  part-I-1Dplots.ipynb
  part-II-1Dplots.ipynb
  part-II-Grid-Convergence.ipynb
  part-II-1Dplots-Sensitivity.ipynb
  part-III-1Dplots.ipynb
  *.session / *.gui
```

**对当前脚本的帮助**
你现在的脚本：

[run_benchmark_experiment_phreeqc_tst.m](</mnt/c/Users/imgw/Documents/Codex/RTSPHEM-main/ReactiveTransport/RTM/run_benchmark_experiment_phreeqc_tst.m:101>)

已经正确抓住了论文 Table 2 的核心设置：单圆颗粒、`0.1 cm x 0.05 cm` 域、`R = 0.01 cm`、`uin = 0.12 cm/s`、`D = 1e-5 cm2/s`、`pH=2`、`kH+ = 10^-4.05`、`Pe=600`、`DaII=178`、`endTime = 2700 s`。我渲染核验了 PDF 的 Table 2/3 页面，脚本这些数值是吻合的。

最有价值的补充材料在：

```text
Results/Part-II/
```

它能提供三类帮助：

1. **官方参考曲线**
   可用于把我们的 `TST` 和 `PHREEQC` 结果，不只互相比较，还和 Molins benchmark 五套代码结果比较。

   重点文件包括：

   ```text
   Part-II/chombo-crunch/partII_grain_volume.csv
   Part-II/chombo-crunch/partII_surface_area.csv
   Part-II/chombo-crunch/partII_average_rates.csv
   Part-II/dissolFOAM/Part-II-volume-time.csv
   Part-II/dissolFOAM/Part-II-area-rate-time.csv
   Part-II/openFOAM/256x128.csv
   Part-II/LatticeBoltzmann/PartII_256x128.csv
   Part-II/Vortex/Diagnostics_cv256.csv
   ```

   这些对应论文官方图中的 grain volume、surface area、average reaction rate。

2. **网格收敛参考**
   `part-II-Grid-Convergence.ipynb` 使用了：

   ```text
   64x32 / 128x64 / 256x128 / 384x192
   ```

   这对我们判断 `cfg.dxfResolutionX = 200`、`cfg.dxfResolutionY = 100`、以及 mesh 参数是否足够很有帮助。官方 Chombo-Crunch 输入里基准网格是 `256 x 128`，和论文 appendix 图里的收敛分析一致。

3. **Pe-Da 灵敏度设计**
   PDF Table 3 和 `part-II-1Dplots-Sensitivity.ipynb` 对应四组：

   ```text
   Pe=600, DaII=178      D=1e-5, kH+=10^-4.05
   Pe=6,   DaII=0.178    D=1e-3, kH+=10^-5.05
   Pe=600, DaII=17800    D=1e-5, kH+=10^-2.05
   Pe=6,   DaII=178      D=1e-3, kH+=10^-2.05
   ```

   这可以扩展当前脚本，从单一基准变成一套小型验证矩阵。

**当前脚本最值得补强的点**
现在 `exportBenchmarkComparison()` 主要比较我们自己的 `TST` vs `PHREEQC`，指标是 porosity、permeability、`k/k0`、rate 等。但官方 benchmark 更看重：

```text
grain volume
surface area
average reaction rate
shape evolution
```

而 `PNM_beauty3.m` 已经在 `global_evolution_log.csv` 输出了：

```text
surface_area_cm2
grain_volume_cm3
avg_dissolution_rate
```

所以最自然的增强是：

1. 增加一个 `loadMolinsPartIIReferences()`。
2. 读取 `Results/Part-II` 中五套代码的 CSV。
3. 做单位和归一化处理：
   Chombo/LB/Vortex 大多可接近直接比较；dissolFoam 有归一化体积、`mm2` 和 `mol/m2/s`；OpenFOAM 看起来是 SI 量级，需要转换。
4. 在 `global_comparison.png` 中叠加官方参考曲线。
5. 在 manifest 中记录 supplementary material 路径、引用的 CSV 文件和单位转换规则。

一句话：补充材料可以把现在的脚本从“内部 TST/PHREEQC 一致性测试”升级为“对 Molins Part II 官方 benchmark 数据的可追溯验证”。

我没有运行 MATLAB benchmark、COMSOL 或任何数据生成批处理；本次只做了 PDF/补充材料/脚本结构检查。