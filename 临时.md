

请将我提供的参考图片高保真重建为一份可编辑 PowerPoint。

这不是代码开发任务，不要开发图片转 PPT 程序，不要依赖 PaddleOCR、Tesseract、OCR token、外部识别 API 或第三方转换服务。请直接使用你自身的多模态视觉理解能力识别图片中的文字、版式、颜色、位置、图形和素材。

## 核心目标

原图是唯一视觉标准（或者你理解了可以在论文里找对应更精准的图）。 

最终 PPT 必须与原图尽可能一模一样，而不是内容相似、风格相似或重新设计。将原图和 PPT 渲染图快速切换时，主要元素不应出现明显的位置、大小、颜色、字体、裁切或素材变化。

在“完全可编辑”和“视觉一致”冲突时，优先保证视觉一致，同时保留合理的分层编辑能力。

## 素材处理

请积极使用你自身的图像生成与图像编辑能力：

[$imagegen](C:\\Users\\imgw\\.codex\\skills\\.system\\imagegen\\SKILL.md)

对于原图中的人物、设备、产品、科研插图、复杂图标、纹理、光效、渐变装饰和其他复杂素材，不要用相似素材替代，也不要粗糙地用基础形状重新绘制。

请按照以下方式处理：

1. 从原图中识别并裁取每个独立素材。
2. 将原图局部作为 imagegen 的直接视觉参考。
3. 优先使用图像编辑，而不是脱离原图重新生成。
4. 使用 imagegen 完成去背景、透明化、边缘修复、遮挡补全、清晰化和局部重建。
5. 输出与原图轮廓、视角、构图、比例、颜色、纹理、光影和细节一致的透明 PNG 或 SVG。
6. 每个独立素材分别放入 PPT，允许单独移动、缩放和替换。
7. 如果第一次生成不像，继续以原图局部为参考调整，不得直接接受风格相似的结果。

imagegen 的作用是复原原图素材，不是创造新的设计。不得增加原图中不存在的内容，不得改变人物、设备、图标或插画的外观。

## 元素重建方式

- 文字：使用 PowerPoint 原生文本框，保持内容、字体视觉、字号、字重、颜色、换行、行距和位置一致。
- 简单矩形、圆形、线条、箭头、色块：使用 PowerPoint 原生 Shape。
- 简单图标和规则矢量图：精确重绘为 SVG。
- 人物、照片、复杂插画、设备、纹理和光效：优先使用 imagegen 编辑成独立透明 PNG。
- 图表和科研示意图：在不改变科学含义的前提下分层重建；无法可靠还原的数据不得编造。

不要把整页图片作为 PPT 背景，也不要用一张大截图覆盖整页。但不要为了过度拆分而破坏素材完整性。

## 排版要求

直接根据原图识别每个元素的坐标、大小、比例、旋转、透明度、颜色和图层关系，并等比例映射到 PPT。

不得重新排版、重新配色、自动美化、调整留白、改变文字换行或替换为相似素材。

## 检查与修正

第一版完成后必须：

1. 将 PPT 渲染为图片。
2. 与原图按相同尺寸叠加比较。
3. 检查文字、素材、位置、尺寸、颜色和裁切差异。
4. 对差异明显的素材再次使用 imagegen 局部编辑和修正。
5. 至少完成一轮“重建—渲染—对比—修正”。

不要只输出方案、代码、命令或执行日志。不要因为缺少外部 OCR 或某个工具而停止。直接使用自身视觉能力和 imagegen 完成重建，并最终交付可编辑的 `.pptx` 文件及独立素材。
