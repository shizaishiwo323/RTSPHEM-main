## 审核结论

我审阅的是该分支当前读取到的快照：benchmark runner blob `7d023a2…`，主程序 `PNM_beauty3.m` blob `8f58bf1…`。

你的 PHREEQC 路径已经形成了一个清楚的：

```text
无反应输运
  → PHREEQC 水溶液平衡
  → 计算 TST 表面速率
  → PHREEQC 加入规定的 CaCO3 反应量
  → 更新浓度
  → 移动 level set
```

因此从程序顺序上，它确实属于 **Lie 型 SNIA**。而且 Molins Part II 的几何、速度、扩散系数、摩尔体积、入口 H⁺ 和速率常数大体都设置正确。

但是，目前有四个会直接影响定量正确性的阻断问题：

1. 输运使用的流体体积与 PHREEQC 使用的流体体积不一致。
2. 输运了自由 (H^+)，而不是守恒化学组分。
3. 当前 `tst_match` 不是 PHREEQC 隐式动力学，而是外部显式速率加 `REACTION`。
4. Part II 的时间层次、初始稳态和几何更新方式与 Chombo-Crunch benchmark 不一致。

在解决这些问题前，PHREEQC 曲线与 legacy TST 曲线接近，并不能证明耦合已经正确。

---

# 一、最严重的问题：输运和化学使用了不同的控制体积

你的 PHREEQC 几何适配器计算了每个三角形内真正的：

[
V_{w,i},\qquad V_{s,i},\qquad A_{\Gamma,i},
]

即通过 level set 切割三角形，得到部分流体面积、部分固体面积和界面长度。

但是 `TransportLEVEL` 的几何处理并不是 cut-cell：

* 只有三个顶点都在固体内的三角形才被排除；
* 只要一个顶点处于流体侧，整个三角形就作为完整流体单元；
* 时间质量矩阵仍使用完整的 `g.areaT`。

于是一个界面三角形中，若真实流体比例为 (\kappa_i)，则：

```text
输运认为的摩尔数 = C_i × V_i
PHREEQC认为的摩尔数 = C_i × κ_i V_i
```

两者相差：

[
\frac{1}{\kappa_i}.
]

如果一个界面 sliver cell 的流体体积分数只有 0.02，输运和化学对同一个浓度所代表摩尔数的理解会相差 50 倍。

这也是 `cut_cell_agglomerated` 只能缓解 PHREEQC 输入过浓、却不能真正解决守恒的原因。化学更新后得到的浓度写回输运场，下一步输运又会把它解释为整个三角形上的浓度。

Chombo-Crunch 的关键并不只是“能算出表面积”，而是 **输运质量矩阵、通量、化学水体积和反应面积全部使用同一套 EB 几何测度**。论文明确指出 cut cell 同时考虑部分流体体积和流固界面面积。

### 建议

有三个可行方向，按严格程度排序：

1. 实现真正的 cut-cell/embedded-boundary 输运；
2. 使用随界面重构的贴体网格，并做守恒 remap；
3. 使用一致的 micro-continuum 方法，在所有方程中统一使用孔隙率和比表面积。

仅给输运方程的 `A` 设置为 (\kappa_i) 还不够。至少还需要：

* 开口边长度或面积分数；
* cut-cell 扩散、对流通量；
* 界面反应通量；
* small-cell 稳定化；
* 几何更新后的守恒 remap。

---

# 二、当前化学状态向量不是守恒组分向量

当前实际运输的是：

```text
[free H+, total Ca, total C, total Na, total Cl]
```

PHREEQC 输出中的 `h_mol_cm3` 来自 `m_H+`，即自由 (H^+) molality；而 Ca、C、Na、Cl 使用的是元素总量。

随后自由 (H^+) 又直接写回输运场。

这会产生一个根本问题：

> 自由 (H^+) 不是酸碱反应中的守恒输运量。

例如：

[
H^+ + HCO_3^- \rightleftharpoons H_2CO_3^\ast
]

会显著改变自由 (H^+)，但不代表有一个空间输运源项。当前做法会把 PHREEQC 的局部 speciation 变化当成下一步可以独立运输的物质量，同时又单独运输总 C，因此酸度或碱度信息没有形成完整的守恒基底。

Chombo-Crunch 的 SNIA 是先运输 total components，再由化学模块求 primary species、secondary species、pH 和矿物动力学；它不是运输自由物种。

### 推荐的 PHREEQC 输运变量

对当前 Ca–C–Na–Cl 系统，可采用：

```text
Ca_total
C_total
Na_total
Cl_total
Alkalinity
```

或另一套经过严格定义、线性独立的守恒 basis。每个 PHREEQC 调用后：

```text
守恒 totals / alkalinity  → 保留并运输
pH、H+ activity、HCO3-   → 只作为局部派生状态
```

pH 只能是化学计算的输出，不应作为一个独立守恒标量继续输运。

还有一个相关问题：当前 PHREEQC 输入把最低 (H^+) 设为 (10^{-7}) mol/L，因此输入 pH 永远不会高于 7。

这不只是数值保护。当真实溶液 pH 高于 7 时，下一步会被重新截回 pH 7，相当于人为添加酸。用于取对数的数值下限应接近机器安全范围，而不是一个有物理意义的中性 pH；更根本的办法仍然是运输 alkalinity，而不是 (H^+)。

---

# 三、`PHREEQC-TST` 实际是显式外部动力学，不是 PHREEQC 隐式动力学

目前 `ComputeTstMatchStepDissolution` 先用步初状态计算：

[
r^n=k,a_{H^+}^n,
]

然后：

[
\Delta n_{\mathrm{cal}}=
r^n A_\Gamma\Delta t.
]

之后 PHREEQC 输入使用的是：

```phreeqc
REACTION
    CaCO3 1
    prescribed_moles moles
```

而不是 `KINETICS`。

因此 PHREEQC 在这里主要承担：

* 添加给定量的 Ca 和 C；
* 重新进行水溶液平衡和 speciation；

它并没有隐式求解：

[
n^{n+1}-n^n
===========

r!\left(a_{H^+}^{n+1}\right)A_\Gamma\Delta t.
]

而论文中 CrunchFlow 化学步的动力学时间积分是 fully implicit backward Euler。

这并不意味着当前做法不能使用，但它应准确命名为：

> explicit external TST rate + PHREEQC equilibrium closure

而不是 PHREEQC implicit TST kinetics。

## 更严重的固体库存不一致

你对 PHREEQC 规定反应量进行了矿物库存截断：

[
\Delta n_{\mathrm{cal}}
\le n_{\mathrm{cal,cell}}.
]

但是 normal speed 没有使用截断后的速率，仍然由原始 vertex rate 计算。

因此，当库存截断发生时：

```text
PHREEQC 溶解的矿物量 < level set 删除的矿物量
```

这会直接破坏固体质量守恒。

必须保证每一步：

[
V_s^{n+1}-V_s^n
+
V_m\sum_i \Delta n_{\mathrm{cal},i}
\approx 0.
]

较稳妥的顺序是：

1. 计算候选界面反应量；
2. 按矿物库存和水相反应容量限制；
3. 根据最终允许的 (\Delta n) 反算受限表面率；
4. 用这个受限表面率同时驱动 PHREEQC 和 level set。

---

# 四、Part II 的时间层次与 benchmark 不一致

完整 benchmark 使用：

```matlab
dt = 54 s
endTime = 2700 s
enableConcentrationCflLimit = false
```

默认网格约为 (256\times128)，特征网格尺寸约：

[
h\approx 3.91\times10^{-4}\ {\rm cm}.
]

因此仅按入口速度估计：

[
\frac{u\Delta t}{h}
\approx
\frac{0.12\times54}{3.91\times10^{-4}}
\approx1.66\times10^4.
]

你的输运是隐式的，因此这不一定导致线性稳定性失败，但它会造成严重的：

* 时间离散误差；
* SNIA splitting error；
* 数值扩散；
* 反应边界层不能及时反馈到输运。

runner 中 smoke 模式认为“时间步小于 residence time 即可”，但 residence time 并不是 SNIA 的充分精度条件。

## Chombo-Crunch 的 Part II 策略

benchmark 中 Chombo-Crunch 对每个几何状态：

1. 将流动松弛至稳态；
2. 用子时间步将 reactive transport 松弛至稳态；
3. 使用稳态界面反应率；
4. 按 boundary CFL 更新几何。

同时：

[
\Delta t_b
\le
f\frac{h}{\max |v_n|},
\qquad f\approx0.25.
]


当前代码只是把 level set 方程分成若干小步，但在这些 level-set 子步之间没有重新计算流场、输运和化学；normal speed 始终冻结。

这只满足 level-set 方程本身的数值稳定要求，不等价于多物理过程的重新耦合。

另外，当前 CFL 使用：

```matlab
1/4 * 1/10 * 1/numPartitionsMicroscale
```

而不是实际网格的 `meshMinLengthForCfl`。

对于当前长宽比，这个表达式对应的最大位移大约是 (0.5h)，而不是论文采用的 (0.25h)。

---

# 五、Part II 没有从 Part I 稳态开始

你的氢浓度初始化实际上是零：

```matlab
hydrogenConcentration.setdata(0, @(t,x) 0);
```

虽然配置参数名叫 `initialHydrogenConcentration`，它主要被用于入口通量。

Molins Part II 的初始状态应当对应 Part I 的长期稳态场。benchmark 明确指出，Part I 约 3 秒达到稳态，5 秒模拟足够；Part II 的初始结果对应 Part I 的长期极限。

当前第一步是：

```text
初始 pH≈7
→ 做一次无反应输运
→ 做一次 PHREEQC 反应
→ 立即移动几何
```

这不等价于先建立稳态反应边界层。

### 应改为

```text
固定初始几何
    ↓
流动收敛
    ↓
transport → chemistry → transport → chemistry ...
    ↓
反应率、出口浓度、场变量均达到稳态
    ↓
将此状态定义为 Part II 的 t = 0
```

---

# 六、界面反应率取邻接单元最小值缺乏物理依据

当前从 triangle rate driver 构造 vertex rate 时，对所有邻接三角形取最小值：

```matlab
vertexRate = min(adjacent_triangle_values)
```

这有几个后果：

* 一个邻接固体三角形中的零值可能把整个顶点速率变成零；
* 速率强烈依赖网格拓扑和三角形方向；
* 它不是流体侧界面浓度；
* 网格加密时未必一致收敛；
* 容易造成界面 grid locking。

Chombo-Crunch 的物理量应在实际 EB 界面或界面质心上评价，而不是取邻近 cell average 的最小值。

更合理的实现是：

1. 找到每个 cut triangle 中的真实零水平集线段；
2. 从流体侧对守恒组分进行 one-sided reconstruction；
3. 在界面线段中心调用 PHREEQC/speciation；
4. 得到界面活度和表面率；
5. 按界面长度积分；
6. 将界面速度沿法向延拓到 level-set 窄带。

---

# 七、局部 agglomeration 会重复使用同一份水相

`computeAgglomeratedReactionWaterVolumes` 为每一个 reactive source 独立搜索附近 pore cells，并把它们放入对应矩阵列；代码没有要求不同 source 的 candidate 集互斥。

因此，一个水相单元可能同时属于多个 reaction agglomerate。你也记录了 `agglomerateMaxRowOverlap`，说明这种重叠是预期会发生的。

问题在于，各 source PHREEQC 计算均使用同一个步初混合状态：

```text
source A 用这份 H+ 反应一次
source B 又用同一份 H+ 反应一次
最后把两个浓度增量叠加
```

在线性、小反应增量极限下近似可接受；在高 Damköhler 数或大时间步下，会重复消费同一酸度库存。

建议将相互重叠的 interface source 合并为 **互不重叠的 connected agglomerates**：

```text
一个 agglomerate
    = 一组水相 cells
    + 与它们相邻的全部界面 reaction moles
    → 一次 PHREEQC 调用
```

这样每份水相在一个化学步内只参与一次局部反应闭合。

---

# 八、当前 PHREEQC 与 legacy TST 的比较不是受控比较

runner 直接比较两条路径。

但它们并不只差一个 chemistry backend：

### PHREEQC 路径

```text
transport first
→ PHREEQC
→ geometry
```

### Legacy 路径

```text
geometry first
→ flow
→ transport + reaction source 联立 Newton
```

`phreeqcTransportFirstSplit` 仅对 PHREEQC `tst_match` 开启。
legacy TST 则把表面源项直接放进 transport Newton 系统。

此外，两者的界面面积表示也不同：

* legacy 使用“两个端点均为固体”的完整网格边长除以三角形面积；
* PHREEQC 使用 level-set 真正切过三角形形成的界面线段。

所以当前差值同时包含：

```text
化学后端差异
+ operator ordering 差异
+ 显式/隐式反应差异
+ 界面面积离散差异
+ 几何更新时刻差异
```

不能把它解释为“PHREEQC 耦合误差”。

应建立一个公共 driver：

```matlab
transportedState = transport.advance(conservedState, dt);
[reactedState, surfaceRate] = chemistry.react(transportedState, geometry, dt);
geometry.advance(surfaceRate, dt);
```

然后只替换：

```text
LegacyTstBackend
PhreeqcBackend
```

其余顺序、时间步、面积、体积、限幅、几何速度必须完全相同。

---

# 九、还有若干守恒和工程问题

## 非守恒截断

`newtonIteration` 对负浓度直接执行：

```matlab
Ui = max(Ui, 0);
```

多个 PHREEQC 写回位置也使用 `max(...,0)`。这会隐藏输运不稳定并改变总摩尔数。正确做法应是：

* 使用守恒 positivity-preserving flux；
* 发生负值时拒绝当前步并减小 (\Delta t)；
* 至少记录被截断的总摩尔数。

## 几何变化后没有明确的守恒 remap

`Continuation` 将浓度用 `max` 复制到固体一侧作为延拓。

这可以用于 level-set 或 stencil 数值延拓，但不能代替守恒 remap。新流体单元出现时，必须处理：

[
N_{k,i}=C_{k,i}V_{w,i},
]

而不是简单复制浓度。

## 现有质量诊断不能证明 PHREEQC 守恒

当前质量诊断：

* 使用只要有一个流体顶点就计入完整三角形面积；
* 只检查 H；
* 反应 sink 仍使用 legacy `SorceScale × k × H`。

因此即使 `mass_res` 很小，也不能证明 cut-cell PHREEQC 多组分质量守恒。

## 入口条件并非严格 Dirichlet

`TransportLEVEL.gF` 是总通量条件，而代码入口采用：

[
q\cdot n=-u c_{\rm in}.
]

benchmark 定义的是入口浓度 Dirichlet。高 Pe 下两者可能接近，但存在扩散时并不严格等价，建议通过一维对流扩散测试验证。

## 单位标签有错误

`rateCoefficientTST` 在输出中标为：

```text
mol dm^-2 s^-1
```

但 Molins benchmark 的 (10^{-4.05}) 单位是：

[
\mathrm{mol,cm^{-2},s^{-1}}.
]

`calciteRate_mol_dm2_s` 实际又被赋值为 `mol/s`，不是面归一化速率。

这些命名将来非常容易产生 100 倍面积换算错误。

## PHREEQC 调用成本

每个 batch 都：

1. 写一个 `.phr` 文件；
2. 创建新的 COM 对象；
3. 重新加载数据库；
4. 运行；
5. 销毁 COM 对象。

建议使用持久化 IPhreeqc 实例和 `RunString`，数据库只加载一次；同时检查 `LoadDatabase`、`RunFile` 返回码和 `GetErrorString`。

---

# 推荐的耦合结构

针对 Molins Part II 的准稳态假设：

```text
初始化几何
    ↓
求稳态流动
    ↓
在固定几何上做 SNIA 子迭代直到 reactive transport 稳态
    ↓
将此状态设为 Part II 的 t = 0

while t < 2700 s

    1. 使用当前几何求稳态流动

    2. reactive-transport relaxation
       repeat
           conservative component transport(dt_rt)
           PHREEQC local reaction(dt_rt)
       until concentration/rate/outlet flux converged

    3. 在真实界面中心评价 surface rate

    4. 选择
       dt_geom <= 0.25*h_min/max(abs(vn))

    5. 使用同一受限反应量：
       chemistry moles
       mineral volume loss
       level-set normal speed

    6. 移动一次几何

    7. 基于 moles 做 conservative remap

    8. 检查水相和固相守恒

end
```

若目标不是准稳态 benchmark，而是一般瞬态问题，则应在一个 geometry macro-step 内对：

```text
transport → PHREEQC
```

进行多次真实时间子循环，并使用时间平均界面速率移动几何。

---

# 必须加入的守恒断言

每个组分 (k)：

[
\epsilon_k =
N_k^{n+1}-N_k^n
-\Delta t(F_{k,\mathrm{in}}-F_{k,\mathrm{out}})
-\nu_k\Delta n_{\mathrm{mineral}}.
]

其中：

[
N_k=\sum_i C_{k,i}V_{w,i}.
]

固体：

[
\epsilon_s =
V_s^{n+1}-V_s^n
+
V_m\sum_i\Delta n_{\mathrm{calcite},i}.
]

另外至少记录：

```text
component mass residuals
solid-volume residual
charge residual
clipped/limited moles
max geometry displacement / h
maximum agglomerate overlap
PHREEQC failed-cell count
```

---

# 推荐的验证顺序

首先只做 Molins Part I，不移动几何。理论初始值及 Chombo-Crunch 参考结果为：

[
A_0=0.0628\ {\rm cm^2},
\qquad
V_0=3.14\times10^{-4}\ {\rm cm^3},
]

[
\bar r_{\mathrm{Chombo-Crunch}}
\approx4.32\times10^{-8}
\ {\rm mol,cm^{-2},s^{-1}}.
]



验收顺序应为：

1. Part I 稳态反应率、pH 剖面和通量加权出口浓度；
2. 网格收敛：例如 (128\times64)、(256\times128)、(512\times256)；
3. SNIA 子步减半收敛；
4. 水相和固相守恒；
5. 最后比较 Part II 在 15、30、45 min 的几何轮廓、面积和体积。

另外应区分两类测试：

```text
Molins benchmark:
    单一 H+ 不可逆反应
    不启用完整碳酸盐 speciation

PHREEQC integration benchmark:
    守恒总组分 + alkalinity
    完整碳酸盐平衡和矿物动力学
```

Molins Parts I/II 明确不包含多组分地球化学和水溶液络合，因此当前启用完整 PHREEQC carbonate chemistry 后，不应再要求它与单一 (H^+) benchmark 逐点完全一致。

按优先级，最先应修改的是：**统一 cut-cell 体积测度、替换自由 (H^+) 输运、统一 chemistry/geometry 的受限矿物量、重构两层时间循环**。这四项不解决，其余参数调优很难得到可解释的结果。
